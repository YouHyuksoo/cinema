# CINEMA 시각 시스템

- 중앙 HUD 프레임·배경·START·선택기는 고정 청록색 대신 공통 `--film-*` 테마를 상속한다. 리액터 본체·코일·평상시 눈·스파크는 기존 Canvas 테마 변환기를 사용한다. 우주선·레이저·폭발·분노한 눈은 원래 연출 색상을 유지한다. 테마 변경은 기존 렌더러 색상만 교체하며 재생 시간·이스터에그·클릭 영역을 초기화하지 않는다. 영향: SignalFilm → JarvisMain → JarvisWave → filmThemeCanvas / drawJarvisVoiceField / components/drawVoiceReactor, jarvisCenterLayout / jarvisCenterBackdrop / jarvisIgnition.module.css. 검증: cinemaCenterTheme.test.ts.

- 리액터 클릭 영역은 Canvas의 중심(50%, 50%)에 놓고 표시 크기 대비 백분율로 환산한다. getBoundingClientRect로 얻은 축소 후 픽셀 좌표를 800×400 기준판의 CSS left/top에 직접 쓰지 않는다. 배경 모드 전환·확대/축소에도 눈 중심과 클릭 영역이 일치해야 한다. 영향: JarvisWave.tsx → reactorTriggerLayout.ts. 검증: cinemaReactorTriggerLayout.test.ts 및 실제 중심 히트 테스트/이스터에그 재생.


- 좌우 스트림 가독성: 부모·카드·설명·게이지 글자에 중첩 투명도를 적용하지 않는다. 본문 #e6f3f6, 보조 문구 #c3d6dc를 스트림 내부에만 사용하고 테마 액센트는 유지한다. 카드 위치·회전·깊이 연출은 유지하되 호버하지 않아도 글자가 밝게 보인다. 끝부분의 스크롤 진입/이탈 마스크만 유지한다. 영향: jarvis.module.css / jarvisStream.module.css.

- 메인 좌우 스트림의 역할을 고정한다. 왼쪽 HELP / SETTINGS는 연결 상태·음성 설정·명령 예시·화면 안내·조작 도움말을 담당한다. 오른쪽 DATA / ANALYSIS는 현장 게이지 → 생산 → 공정 → 병목 → 품질 → 전력/효율 → 제품 검사 → 온도 알림 순서로 데이터 카드를 모은다. 동일한 분석 카드를 양쪽에 복제하지 않으며 기존 연출 연결·스크롤·호버 정지는 유지한다. 영향: JarvisMain.tsx → JarvisHelp.tsx / JarvisOperations.tsx / JarvisStream.tsx. 검증: cinemaStreamRoles.test.ts.

- 네온 HUD 카메라는 우상단 프레임 내부 안전영역(x 75~95%, y 14~33%)에 제한하고 영상을 내부에서 클리핑한다. 경계선을 가리던 OPERATOR / VISUAL LINK 및 영상 상태 장식 문구는 숨기며 연결/끄기 버튼과 접근성 이름은 유지한다. 기존 스타일에는 적용하지 않는다. 영향: jarvisCenterLayout.module.css.

- 네온 HUD 전체는 800×400 단일 기준판(sceneSurface)에 배경·리액터·모델정보·카메라·START·배경 선택기를 함께 배치한다. 바깥 scene의 가용 너비/높이 중 작은 배율로 등비 확대·축소하며 남는 공간은 중앙 정렬한다. 모바일에서 자식 글자·버튼만 별도 크기로 바꾸거나 숨기지 않는다. HTML 버튼/선택기의 의미·포커스·이벤트와 자식 마운트는 유지한다. 기존 스타일은 display:contents로 기존 레이아웃을 유지한다. 이전 START 단독 44px 축소 규칙을 대체한다. 영향: JarvisCenterLayout.tsx / jarvisCenterLayout.module.css.

- 네온 HUD 배경 선택기는 불투명 입력 박스 대신 투명한 바탕, 네온 글자, 얇은 밑선과 CSS 화살표로 프레임에 통합한다. 우하단 안전영역(x 75~95%, y 73~91%) 안에 두고 native select의 키보드·선택·저장 동작은 유지한다. 기존 스타일의 선택기는 변경하지 않는다. 영향: jarvisCenterLayout.module.css의 .hud .backgroundPicker.

- 네온 HUD 모델정보는 좌상단 프레임의 사선과 장식선을 피한 내부 안전영역(x 5~25%, y 14~33%)에 둔다. 긴 모델명은 줄바꿈하며 넘치는 세로 내용만 박스 안에서 스크롤한다. 제목은 한 줄 말줄임, 가로 넘침은 차단한다. 영향: jarvisCenterLayout.module.css의 .hud .aiPanel.

- 중앙 보호 너비(2026-09-09): 데스크톱 중앙 열은 최소 480px를 확보하고 남는 너비를 우선 사용한다. 양측 스트림은 각 140~200px, 화면 1100px 이하에서는 120~170px로 먼저 축소한다. 900px 이하의 중앙 우선 세로 배치는 유지한다. 영향: jarvis.module.css의 .body 및 1100px 반응형 규칙.

- 중앙 배경 선택기 위치 최종 교정(2026-09-09): 네온 HUD에서는 오른쪽 하단 프레임을 `OPTIONS / DISPLAY` 배경 선택 박스로 사용한다. 기존 스타일에서는 상단 선택기를 유지한다. 동일한 select 한 개를 CSS로 재배치하여 전환 중 포커스·리액터·카메라를 유지한다. 네온 모드의 상단 선택 행은 없애고 그 공간을 스테이지에 돌려준다. 아래의 상단 선택 고정/우측 하단 빈 슬롯 규칙은 이 기준으로 대체한다.

- 중앙 배경 선택(2026-09-09): 중앙 상단의 `중앙 배경` 선택으로 `기존 스타일` / `네온 HUD`를 전환한다. 기존 스타일이 기본이고 선택은 브라우저 로컬 저장소 `cinema.center.background.v1`에 보관한다(저장 불가 환경에서도 현재 세션 전환은 가능). 참조 이미지의 네 모서리 절단 프레임·중앙 분절 링 구조만 SVG/CSS로 재구성하며 이미지·워터마크는 사용하지 않는다. 사용자 색상 교정에 따라 붉은색 대신 시안/블루 네온과 어두운 남색을 쓴다. 네온 HUD 상단 왼쪽은 AI 상태/음성·텍스트 모델명, 상단 오른쪽은 카메라, 하단 왼쪽은 START/STOP, 하단 오른쪽은 빈 장식 슬롯이다. 모델명은 좁은 박스에서 줄바꿈하고 모바일 START는 최소 44px를 유지한다. 선택부·상태 문구·답변·입력은 스테이지 밖 독립 행이며 HUD 테마 변수는 scene에만 제한한다. 배경 전환은 기하·CSS만 바꾸고 카메라/리액터 컴포넌트를 재마운트하거나 장치를 시작하지 않는다. 영향: jarvisCenterBackground.ts(선택·저장) → JarvisCenterLayout.tsx / jarvisCenterLayout.module.css(배치) → JarvisCenterBackdrop.tsx / jarvisCenterBackdrop.module.css(장식). 검증: cinemaCenterBackground.test.ts, cinemaCenterLayout.test.ts 및 실제 브라우저 전환 중 이스터에그 단계 유지.

- 중앙 시선 우물(2026-09-09): 메인 중앙 음성 영역은 좌우 스트림·상단 지표·하단 도크와 같은 평면이 아니다. 절단 모서리 시안 프레임·안쪽 코너 브래킷·리액터를 감싸는 타원 링으로 우물을 만들고, 좌우 스트림은 기본 불투명도 0.62·채도 0.68로 한 단계 낮춘다. 스트림에 호버·초점이 오면 읽기 위해 되돌린다. 중앙만 액센트 방사 그라데이션을 받는다. 전면 글래스나 보라 그라데이션은 쓰지 않는다. 영향: jarvis.module.css(.body > :not(.center) · .center clip-path · .wave::before). 검증: cinemaDialogue.test.ts.

- 리액터 클릭 이스터에그(2026-09-09 확장): 20초 장난극. 첫 접근/장난 → 좌우로 고개를 흔들어 쫓음(2.2~3.25초) → 도망/재등장 → 머리 위 착륙·까딱거림(6~7.8초) → 더 세게 털어냄(7.8~8.8초) → 세 번째 방문/도발 → 12.2초부터 중앙 눈만 붉고 가늘어지며 내부 불꽃 5개가 이글거림 → 14.1초 도주/뒤쪽 가림 → 15.2초 급선회 → 16.6초 발사/16.84초 명중 → 18.3~20초 원래 시안 눈과 회전으로 복귀. 착륙 시 엔진을 끄고 착륙 다리를 내리며 우주선은 본체의 끄덕임·흔들림을 따라간다. 금속·코일 색은 유지하고 불꽃은 눈 윤곽 안에만 제한한다. 클릭 영역은 리액터 크기의 투명 버튼, 재생 중 중복 클릭 무시, 버튼 Escape/페이지 숨김/언마운트에서 취소한다. 동작 줄이기는 1.2초 정적인 우주선 인사만 표시한다. 영향: reactorShipFlight.ts(경로·시간) → reactorEasterEgg.ts(표정·조준·착륙·재생 수명) → components/drawReactorEasterEgg.ts / drawReactorEyeHeat.ts / drawVoiceReactor.ts → drawJarvisVoiceField.ts(앞뒤 합성) → JarvisWave.tsx / jarvisWave.module.css. 검증: cinemaReactorEasterEgg.test.ts, cinemaReactorEyeHeat.test.ts.

- 리액터 후면: 평면 캡 대신 본체와 함께 회전하는 메카닉 조립체. 높낮이가 다른 장갑 6개·실제 깊이가 있는 냉각 홈 18개·굵은 구리 배관 3개·육각 볼트 6개·깊은 베어링·돌출 육각 구동축을 불투명 면으로 표현한다. 건메탈/은색이 주조이고 시안 표시는 작게 제한한다. 앞면 방향에서도 후면 부품을 먼저 그린 뒤 본체로 가려, 옆으로 도는 동안 돌출축이 갑자기 나타나지 않게 한다. 후면 면은 깊이순 정렬하며 와이어 선을 추가하지 않는다. 영향: reactorRearGeometry.ts(정적 mesh·공유 회전/투영) → components/drawReactorRear.ts → drawVoiceReactor.ts. 검증: cinemaReactorRear.test.ts의 구조·깊이·회전 경계 실루엣·그리기 순서.

- 음성 리액터 최종 기준(2026-09-09 사용자 교정): 유리 케이스 없이 두께와 명암이 있는 금속 아크 리액터 자체를 기울어진 축으로 회전시킨다. 논리 중심은 440×320 안에서 y 134로 조금 위에 둔다. 8개 넓은 발광 코일·3개 굵은 지지대·밝은 플라즈마 코어가 같은 시선 좌표를 사용한다. 원통 두께는 앞뒤 80(±40)이며, Prometheus 엔지니어 문양은 원통 옆면에만 새기고 앞면 안쪽 링에는 넣지 않는다. 톱니·터빈 날개는 쓰지 않으며, 문양은 칼라 각으로 천천히 돈다. 시선 피치는 고정하지 않고 천천히 위를 보다가 아래를 보는 듯 끄덕이며, 코어는 코일과 같이 돌지 않고 몇 초마다 눈꺼풀처럼 세로로 감았다 뜬다(가끔 두 번). 실제 마이크/AI 출력 음량에 따라 코어 밝기·크기와 최대 5갈래 테슬라 스파크가 반응하며, 방전은 코어와 바깥 코일 사이에서 발생한다. 무음·대기·답변 준비·오류·동작 줄이기에서는 스파크를 끄고, 동작 줄이기에서는 피치를 원래 기울기(.27)에 고정하고 깜박임을 끈다. 얇은 격자·궤도·외부 입자·전체 화면 점멸은 사용하지 않는다. 상태 문구는 리액터 아래 독립 행에 두고 440×320 논리 공간을 캔버스 CSS 크기에 균일 contain(최대 0.82배)으로 투영한다. 화면·하단 메뉴로 스테이지가 줄면 같이 줄고, 큰 화면에서 START–카메라를 가득 채우지 않는다. START·카메라 툴바는 스테이지 위에 겹친다. 모바일 중앙 높이는 480px다. 영향: jarvisVoiceCore.ts(상태) → voiceReactorGeometry.ts(공유 회전·투영·방전) → components/drawVoiceReactor.ts(금속 두께·코일·빛) → drawJarvisVoiceField.ts → JarvisWave.tsx; JarvisMain.tsx → JarvisCenterLayout.tsx / jarvisCenterLayout.module.css(효과·상태 행). 아래의 구형 음성 코어·육각 유리 케이스·중앙 50% 상태 제목 규칙은 이 기준으로 대체한다.

- 입체 코어 모바일 배치: 900px 이하 중앙 영역 높이는 480px로 확보해 상단 제어와 답변 사이에 구체가 충분히 보이게 한다. 좌우 정보 스트림은 기존 세로 스크롤 흐름을 유지하며 중앙 자체는 스크롤하지 않는다. 영향: jarvis.module.css의 .body > .center.

- 중앙 음성 코어(2026-09-09): 승인된 VOICE CORE 이미지의 투명 구형 격자·교차 궤도 리본·중앙 빛·입자를 실제 Canvas 3D 원근 좌표로 표현한다. 시안 #6be5ff / 아이스 #d9faff를 기본으로 말하기 피크는 드라큘라 핑크 #ff79c6, 답변 준비는 #b5a2ff를 사용한다. 대기에는 느린 호흡, 듣기에는 유입 입자와 마이크 파형, 답변 준비에는 응축·회전, 말하기에는 출력 오디오 RMS 기반 팽창·파형을 적용한다. 기존 이미지의 상태 비교 썸네일은 화면에 추가하지 않는다. 800×460 논리 공간을 균일 축소하고 뒷면→코어→앞면 순서로 투명 깊이를 표현한다. 중앙 상태 제목·상단 START/카메라·답변·질문 입력은 기존 위치를 유지한다. 기기 접근은 기존 명시적 시작에만 남기고, 렌더러가 장치를 열거나 음성을 전송하지 않는다. 동작 줄이기는 시간·음량 변형을 정지하고 상태별 색만 남긴다. 숨겨진 탭은 RAF를 중지한다. 영향: jarvisAudio.ts / useJarvisVoice.ts(기존 입력) → JarvisWave.tsx(오디오·프레임 수명) → jarvisVoiceCore.ts(상태·투영) → drawJarvisVoiceField.ts(표현). 이 규칙은 아래의 13개 수평 리본 파동 설명을 대체한다.

- 펼친 링 메뉴와 축소 구체는 동일한 드라큘라 핑크 팔레트(#ff79c6)를 공유한다. 육각면 테두리·아이콘·광채·포커스 링은 핑크, 면은 어두운 반투명(#282a36)으로 유지한다. 접힌 구체는 원형 실루엣의 완전한 구에 가는 반투명 축구공 격자 텍스처를 입혀 그리고, 16개 육각 장면 메뉴는 같은 정사영·회전으로 그 칸 중심에 붙인다. 아래 안내 문구는 두지 않는다. 장면 테마에는 영향을 주지 않는다. 영향: filmMenuRing.module.css의 .menu 지역 변수를 링과 구체가 함께 상속; 회귀: cinemaMenuGlobeMarkup.test.ts.

- PCB 정면 원근 구도(2026-09-09 사용자 교정): 좌우 회전을 없애 앞변과 뒤변을 수평·좌우 대칭으로 유지한다. 화면 아래 앞변이 크고 화면 위 뒤변이 작아지도록 깊이 방향을 설정한다. 부품 높이는 화면 위로 솟고, 바닥 링·회로·포커스가 동일 투영을 공유한다. 기존의 비스듬한 좌우 사선 구도는 이 기준으로 대체하며 등장 이후 고정 규칙은 유지한다. 영향: pcbInspectionLayout.ts; PC/모바일 앞뒤 폭 비율·수평·대칭·높이 회귀는 cinemaPcbEntrance.test.ts.

- PCB 추가 시각 기준(2026-09-09): 기판은 테이블 위에 눕힌 것처럼 낮은 수평 사선으로 표현한다. 0~4초 등장에는 바닥 빛 링, 외곽선 전개, 회로 점등, 부품 정착 액션을 쓰되 4초 이후 검사 동안 기판과 부품은 고정하고 포커스만 이동한다. 등장 효과 역시 시간으로부터 결정적으로 파생하며 데이터 값/검사 결과는 변경하지 않는다. 전체 화면 고속 점멸은 사용하지 않는다. 영향 경로: pcbEntrance.ts → components/drawPcbEntrance.ts / components/drawPcbAssembly.ts → drawPcbInspectionFilm.ts; 고정 투영은 pcbInspectionLayout.ts. 검증: cinemaPcbEntrance.test.ts / cinemaPcbFilm.test.ts.

- PCB 시각 재정의(사용자 실화면 피드백): 불투명 초록 보드와 부품을 향한 카메라 이동·확대는 사용하지 않는다. 투명한 유리/와이어 기판을 큰 고정 사선 시점으로 유지하고 포커스 타깃과 연결선만 불량 부품 사이를 이동하며 검사 정보를 표시한다. 시간 변화로 기판·부품의 투영 위치나 배율을 바꾸지 않는다. 아래 초기 PCB 기준의 카메라 접근·후퇴 부분은 이 규칙으로 대체한다. 데이터 값과 자동차 수동 옵션은 그대로 유지한다.

- PCB 불량 분석(machine) 구현 기준: 기본 대상은 SMT PCB, 자동차는 연출 설정에서 명시적으로 선택할 때만 표시한다. PCB 순회 종료나 전체 연속 재생이 대상을 자동차로 바꾸지 않는다. 기존 machine ID·36초·16개 메뉴/538초 순서는 유지한다. 부품은 참조번호 ID를 가진 배열이며 기판·부품·불량 타깃이 같은 3D 좌표/투영을 공유한다. 0~4초 전체 스캔, 4~28초 불량 부품 접근·판독·후퇴, 28~34.5초 전체 집계, 36초 종료다. 정상/불량/미검사 집계는 전달 데이터에서만 파생하고, 시연 형상/판정과 실제 검사 출처를 구분한다. 세로 화면에서는 보드와 판독 영역을 위아래로 재배치한다. 아래 자동차 전용 machine 설명은 수동 자동차 옵션의 보존 규칙으로 적용한다. 영향 경로: pcbInspectionData.ts / pcbInspectionFields.ts → pcbInspection.ts / sceneDataRegistry.ts → pcbInspectionLayout.ts / components/drawPcbAssembly.ts / components/drawPcbInspectionReadout.ts → drawPcbInspectionFilm.ts / drawTransparentMachineFilm.ts → drawSignalFilm.ts / useFilmPlayback.ts → FilmMachineControls.tsx / FilmControls.tsx. 상세 승인 명세: docs/superpowers/specs/2026-09-08-smt-pcb-defect-film-design.md.

- DB 데이터 소스 연동과 관리 화면(/cinema/admin): 데이터 소스·피드 매핑·실행 상태를 서버 설정 파일에 두고 HUD 토큰(--hud-*)과 기존 폰트 변수만으로 구성한 관리 화면에서 편집한다. 새 폰트·새 색상 없음, 본문은 자체 스크롤(body overflow hidden), 상태는 초록/호박/빨강 토큰으로만 구분. 피드 러너가 SELECT만 실행해 서술자로 검증한 뒤 장면 문서로 바꾸고 브라우저가 /api/cinema/feed를 폴링한다. 정적 배포에서는 관리 화면이 안내 문구만 보이고 폴링은 404에서 조용히 멈춘다. 영향 경로: feedConfig.ts → feedMapping.ts / feedScenes.ts → src/server/cinema/hatcheryConfig.ts / oracleSource.ts / feedRunner.ts → src/app/api/cinema/admin/* / feed/route.ts → feedPolling.ts → useFilmPlayback.ts; 화면은 src/app/cinema/admin/page.tsx → src/cinema/admin/HatcheryAdmin.tsx / hatcheryAdmin.module.css. 검증: cinemaFeedModel·cinemaFeedServer·cinemaFeedPolling 테스트, 실 DB(SVEHICLEPDBEXT) 미리보기.
- HATCHERY 값 변경과 객체 기대 포맷: 음성·텍스트·실시간 명령은 모두 표준 객체 패치(source hatchery)로 값을 바꾸고, 정형 문장은 브라우저에서 저장소 데이터 기준으로 해석한다(hatcheryTargets.ts). 객체 값의 기대 포맷은 필드 서술자(sceneField.ts, sceneFields.ts) 한 곳에 선언하고 패치 검증·도구 스키마·응답 단위가 여기서 파생된다. 막대 라인은 첫 객체 타입 모듈(productionLineObject.ts)로 서술자·normalize·describe·layout·draw를 묶었고, 그리기 호출 지문 테스트로 화면이 바뀌지 않았음을 고정한다. 영향 경로: sceneField.ts → productionLineFields.ts / sceneFields.ts → productionLineObject.ts → drawBarFilm.ts; hatcheryTargets.ts → useJarvisLocalVoice.ts / useJarvisVoice.ts / jarvisRealtimeSession.ts / src/server/cinema/openai.ts → JarvisMain.tsx → SignalFilm.tsx(actions). 검증: cinemaBarFilmFingerprint·cinemaSceneField·cinemaProductionLineObject·cinemaHatcheryTargets 테스트.
- 중앙 대화 영역의 별도 HATCHERY 네온 로고는 표시하지 않는다. AI 연결 상태와 모델명은 중앙 영역 상단 전체 폭의 독립 행에서 가운데 정렬하고, 바로 아래 행에 START(좌측)와 카메라(우측)를 둔다. 좁은 화면에서도 AI 정보와 제어가 겹치지 않으며 VOICE/TEXT 모델명은 한 줄을 유지하고 넘치면 가로 스크롤한다. 하단에는 질문 입력창만 남겨 로고 예약 공간을 없앤다. START 내부 표기와 대화 주체명은 유지한다. 아래의 중앙 하단 로고·우측 하단 AI 정보 배치 규칙을 대체한다. 영향 경로: JarvisCenterLayout.tsx / jarvisCenterLayout.module.css / jarvisAiStatus.module.css. 검증: cinemaHeaderIgnition.test.ts의 로고 제거·AI 정보 단일 표시·상단 순서.

- 장면 데이터 계약: 모든 장면은 docs/standards/scene-data-contract.md의 봉투(scene·version·source·at·data)와 객체 패치(id 기준, id·label 보호)로만 데이터를 받는다. 객체는 도메인 코드 id 배열, 값은 숫자와 단위, 개수 무관 배치, 시간은 연출에만, 오류는 valid:false. 재생 훅이 저장소를 소유하고 applySceneDocument/applySceneObjects/sceneProvenance를 노출하며 마지막 갱신이 이긴다. 정적 JSON은 public/cinema/data/scenes.json(없으면 기본 시연 데이터). 영향 경로: sceneDataDocument.ts → sceneDataRegistry.ts → sceneDataStore.ts → useFilmPlayback.ts → drawSignalFilm.ts(막대·파이·환경·공정망·SPC·에너지·제품검사 renderer) / environmentSelection.ts(환경 프레임) / jarvisMainData.ts(hatcheryMetrics) / staticSceneData.ts(정적 JSON 어댑터). 검증: cinemaSceneDataDocument·Registry·Store·FilmSceneData·HatcheryMetrics·StaticSceneData 테스트.
- 온습도 연출은 화면 가로/세로 비율이 0.85 미만이면 세로형 440×1040 논리 배치를 사용한다. 10개 ZONE은 2열 5행, 온도·습도 계기는 상단에 배치하며 계기 전체를 균일 확대/축소해 글자와 링의 비율을 유지한다. 21.5~23초에 카드를 위로 재배치하고 각 카드 아래에 공통 눈금의 24시간 그래프를 전개한다. 히트맵은 기존 공장 비행·센서 보간을 상단 공간도에 유지하고 아래에는 읽기 쉬운 구역별 온도와 같은 온도 범례를 표시한다. 바닥 앵커와 선택 안내 공간을 제외하고 배치하며 데스크톱/가로 화면은 기존 구성을 유지한다. 영향 경로: environmentMobileLayout.ts(공유 세로 배치·DPR 독립 변환) → drawWaveFilm.ts / components/drawEnvironmentMobile.ts(세로 화면 조립), environmentSceneObjects.ts → environmentSelection.ts → useEnvironmentSelection.ts / EnvironmentZoneInteraction.tsx(동일 좌표로 터치 선택). 원본 데이터·자동 순회·역방향 탐색·선택 단계는 바꾸지 않는다. 검증: cinemaEnvironmentMobile.test.ts의 화면 경계·카드/그래프 간격·DPR 1/2/3 선택·주입 데이터·가로 배치 유지.

- 우측 하단 VOICE/TEXT 모델명은 오른쪽 정렬된 한 줄로 표시한다. 좁은 화면이나 긴 모델명도 줄바꿈하지 않으며 공간을 넘으면 가로 스크롤·키보드 초점으로 전체 모델명을 확인한다. 연결 상태는 기존 윗줄을 유지한다. 영향 경로: JarvisAiStatus.tsx / jarvisAiStatus.module.css.

- 하단 메뉴 개방 상태는 메인/장면 화면과 독립된 단일 `menuOpen` 상태다. 페이지 최초 진입·새로고침 시에는 접힌 구체로 시작하고, 명시적인 메인 복귀·카메라 활성화 시 열리며 장면 진입 시 접힌다. 접히면 기존 사각형 앵커 대신 링이 변형된 메뉴 구체와 `하단 메뉴 펼치기` 버튼만 남는다. 상태·재생·설정 작업 패널만 inert/aria-hidden이 되고 구체는 그 밖의 형제이므로 항상 조작 가능하다. 펼친 작업 행의 `메뉴 축소`, Escape, 도크 안 장면 실행은 구체로 초점을 옮기고, 구체로 펼친 뒤 변형이 끝나면 정면 메뉴로 옮긴다. 전환 중 사용자가 도크 밖 입력에 둔 초점과 최초 진입 초점은 빼앗지 않는다. Escape는 설정을 먼저 닫고 설정 버튼으로 복귀한 다음 메뉴를 접는다. 구체는 메뉴만 펼치며 재생·장면 선택·카메라를 시작하지 않는다. 영향 경로: SignalFilm.tsx(menuOpen·화면 전환) → FilmDock.tsx(상태 동작·초점 의도·작업 패널) → FilmChapterMenu.tsx / useFilmMenuGlobe.ts(구체 변형 완료 통지) → filmDock.module.css / film.module.css(레이아웃). 검증: cinemaPersistentDock.test.ts 및 실제 메인/장면 선택·구체 재개방·연속 Escape·외부 입력 초점 확인.

- 대화 준비 등 중앙 상태 제목은 중앙 음성 대화 영역의 가로·세로 50% 교차점에 표시한다. 제목 자체를 정확히 가운데 정렬하며 보조 SESSION/STANDBY 표시는 제목 아래에 둔다. 별도 행을 차지하지 않는 포인터 비활성 오버레이로 파동 위에서 읽히게 하고 START·카메라·입력창 위치는 유지한다. 기존 START 아래 상태 제목 배치를 대체한다. 영향 경로: JarvisCenterLayout.tsx / jarvisCenterLayout.module.css / jarvis.module.css. 검증: 실제 제목 중심과 중앙 영역 중심 좌표 비교.

- 중앙 우측 하단에는 AI 연결 상태와 서버 설정 음성/텍스트 모델명을 표시한다. 기존 GET /api/cinema/assistant의 모델 값을 사용하며 하드코딩하지 않는다. 키 설정 완료는 연결 대기, Realtime 데이터 채널 열림은 음성 연결됨으로 구분하고 네트워크 단절/종료 시 연결 표시를 즉시 해제한다. 미설정·조회 실패·연결 중·오류도 별도로 표시한다. 입력창은 하단 첫 행 전체 폭, 다음 행은 좌측 로고/우측 AI 상태로 배치한다. 기존 로고 우측 입력창 규칙을 대체한다. 영향 경로: useJarvisVoice.ts / jarvisRealtimeSession.ts → JarvisAiStatus.tsx → JarvisMain.tsx / JarvisCenterLayout.tsx / jarvisCenterLayout.module.css.

- 상단 헤더는 8개 지표 카드만 메인 화면 전체 폭으로 사용한다. 카드 높이와 한 줄 가로 스크롤은 유지한다. 중앙 음성 대화 영역의 좌측 상단에는 START/STOP과 상태, 우측 상단에는 카메라, 좌측 하단에는 HATCHERY 네온 로고를 배치한다. 입력창은 하단 로고 우측에 독립 그리드로 배치하여 겹치지 않게 한다. 좌우 정보 스트림과 하단 링은 유지하며 장치 시작/종료 콜백은 JarvisMain에 남긴다. 이전 헤더의 START·카메라 및 START 아래 로고 배치 규칙을 대체한다. 영향 경로: JarvisMainHeader.tsx / jarvisHeader.module.css → JarvisMain.tsx → JarvisCenterLayout.tsx / jarvisCenterLayout.module.css. 검증: cinemaHeaderIgnition.test.ts 및 전체 폭·모서리·입력창의 실제 화면 경계.

- 장면 객체 식별자와 막대 장면 데이터 주입: 장면 객체는 도메인 코드 id와 표시용 label을 가진다(UUID·계층형 문자열 id 금지, 빈/중복 id는 정규화에서 제거). 막대 장면은 생산 스냅샷(단위·라인당 목표·라인 1~20개·선택 id)을 주입받아 그린다. 선택 막대는 selectedId 우선, 없으면 목표 미달 중 달성률 최저, 모두 달성이면 선택 없이 상세 패널을 건너뛴다. 눈금은 최대값·목표에 15% 여유를 둔 보기 좋은 값이며 슬롯 폭 70px 미만이면 헤더를 축약한 compact 모드로 전환한다. 파이 장면은 기본 스냅샷 파생값(chartData.ts)을 그대로 읽는다. 영향 경로: sceneObject.ts → productionSnapshot.ts(모델·선택 규칙·눈금) → filmSceneData.ts(주입 통로) → useFilmPlayback.ts(sceneData/updateSceneData) → drawSignalFilm.ts bars renderer → drawBarFilm.ts → barTelemetryGeometry.ts(밀도) → components/drawBarChart.ts. 검증: cinemaSceneObject.test.ts, cinemaProductionSnapshot.test.ts, cinemaBarTelemetry.test.ts, cinemaBarFilm.test.ts, cinemaFilmSceneData.test.ts.
- 최상단 HATCHERY / MAIN INTERFACE 및 우측 상태 문구 행을 제거한다. 같은 전역 행의 장면명/반복 시간도 렌더링하지 않으며 재생 정보는 기존 하단 도크/설정에서 확인한다. 메인 화면 상단 여백은 54px에서 12px로 줄이되 기기 safe-area 상단 값을 보장한다. START 아래 네온 로고·카드·카메라·하단 메뉴는 유지한다. 영향 경로: SignalFilm.tsx → film.module.css / jarvis.module.css.

- HATCHERY 네온 로고는 좌측 START/STOP 버튼 아래에 배치한다. 버튼 중심과 로고 중심을 맞추고 상태 문구는 버튼 우측에 유지한다. 로고는 최대 140×36px이며 중앙 상단의 별도 로고 행은 제거한다. 모바일도 첫 행에 START·로고와 카메라, 둘째 행에 8개 지표를 둔다. 앞선 상단 중앙 로고 규칙을 대체한다. 영향 경로: JarvisMainHeader.tsx / jarvisHeader.module.css. 검증: cinemaHeaderIgnition.test.ts 및 데스크톱/모바일 실제 좌표.

- 메뉴 관리 루빅 큐브는 장면 구체와 별개로 화면 왼쪽 상단에 고정된다: 메인 화면에서는 상단 지표 스트립의 왼쪽 끝에 세로 중앙으로 도킹하고, 스트립은 `--hatchery-cube-space`(투영 폭 1.38×모서리 + 60px 여유; 카드 레일 틀은 비운 폭 바로 뒤에서 시작하고 카드·바는 틀 안쪽 16px에서 시작)만큼 왼쪽 여백을 비워 카드 폭을 그만큼 줄인다. 등각 투영이 박스보다 넓으므로 큐브는 베이 폭의 중앙에 놓는다. 스트립은 두 영역을 각각 1px `--film-line` 틀(모서리 10px 컷)로 구분한다: 왼쪽 메뉴 베이(폭 `--hatchery-cube-bay` = 투영 폭 + 24px, 큐브가 도킹된 `:root[data-cube-docked]`일 때만 표시, 옅은 액센트 배경)와 오른쪽 카드 레일. 스트립이 없는 장면 화면에서는 왼쪽 상단 구석(16px 여백)에 둔다. 드래그로 옮기지 않으며 클릭은 앞으로 메뉴 열기에 쓴다. 계산: filmMenuCube.ts(cubeDockCenter·cubeStripSpace·CUBE_PROJECTED_HALF) → FilmMenuCubeView.tsx(250ms마다 스트립 위치 재측정) → jarvisMetricCards.module.css(.strip padding-left). 검증: cinemaMenuCube.test.ts.
- 큐브 연출: 마운트 직후 스티커는 액센트 색 와이어프레임의 투명 HUD 상태로 나타나고 450ms 뒤 색이 차오른다(스티커마다 위치 기반 0~650ms 지연으로 물결처럼). 유휴 6초마다 Y축으로 90° 돌아(1.1초, ease-in-out) 앞 → 왼쪽 → 뒤 → 오른쪽 면을 차례로 앞으로 가져오며, 새로 드러나는 면은 회전 35% 시점부터 같은 HUD 채움으로 색이 들어온다. 호버·층 회전·섞기 중과 동작 줄이기에서는 돌지 않는다. 계산: filmMenuCube.ts(cubeShowcaseFace·cubeStickerDelay·CUBE_SHOWCASE_*·CUBE_HUD_HOLD_MS) → FilmMenuCubeView.tsx(showcase·data-hud) → filmMenuCube.module.css(.tile 전환·[data-hud]). 검증: cinemaMenuCube.test.ts.
- AI 설정 화면(/cinema/ai): 큐브 메뉴의 AI 타일에서 연다. 프로바이더(OpenAI API·ChatGPT 구독(Codex 로그인)·Anthropic·Gemini)와 모델(카탈로그 또는 직접 입력), API 키 또는 Codex 로그인 상태, 음성 방식(OpenAI Realtime / 브라우저 음성 + 텍스트 모델), temperature·최대 출력 토큰, 운영자 프롬프트 지시어, 접속 테스트를 한 화면에 둔다. ChatGPT 구독은 `~/.codex/auth.json`(CODEX_HOME)의 Codex CLI 토큰을 서버가 읽어 `chatgpt.com/backend-api/codex/responses`에 스트리밍 Responses 요청을 보내고 만료 시 OAuth refresh로 갱신한다(server/cinema/codexAuth.ts). 브라우저 음성 방식이거나 Realtime을 쓸 OpenAI API 키가 없으면 useJarvisVoice가 useJarvisLocalVoice(브라우저 인식·합성 + 텍스트 모델)로 대화하고, 메인 화면의 음성 설정·안내 문구도 그 방식을 따른다. 메인 화면 VOICE 패널의 음성 방식 토글(JarvisVoiceModeToggle.tsx, jarvisVoiceMode.module.css)은 PATCH /api/cinema/admin/ai로 voiceMode만 저장하고 상태를 다시 읽는다. Realtime 선택지는 OpenAI API 키가 없으면 비활성이고, 세션 중에는 바꿀 수 없다. 좌측 스트림의 "AI / 모델 선택" 블록(JarvisAiProviderSelect.tsx, 같은 CSS 모듈)은 같은 PATCH로 provider·model을 바꾼다. GET /api/cinema/assistant가 selectedProvider·selectedModel과 프로바이더별 준비 여부(providers[].ready: 저장된 키·OpenAI 환경 변수·Codex 로그인)를 내려주며, 준비되지 않은 프로바이더는 비활성 버튼으로 두고 AI 설정 화면 링크를 안내한다. 프로바이더를 오갈 때 이전 키는 `ai.apiKeys` 보관소로 옮겨져(aiConfig.ts mergeAiKey/aiKeyVault) 다시 선택하면 그대로 돌아온다. 시스템 프롬프트는 AI 설정 화면 5번에서 통째로 편집한다(jarvisPrompt.ts DEFAULT_JARVIS_PROMPT, `ai.prompt`에 저장, 기본값과 같으면 빈 값). 지시문은 역할·성격과 말투·언어·답변 길이·규칙·도구 섹션 뒤에 운영자 추가 지시, 마지막에 참고 데이터(연출 목록·객체 카탈로그·시연 스냅샷)를 서버가 붙이는 구조이며 `{{목소리}}` 자리에는 선택한 목소리(남성/여성)의 묘사가 들어간다. Realtime 세션은 realtimeConfiguration(voice)이 음색으로 성별을 역산해 같은 지시문을 넘긴다. START 제목 옆에 현재 방식(REALTIME / BROWSER VOICE)을 표시한다. 데이터 소스 관리와 같은 hatcheryAdmin.module.css 톤을 쓰고 서버 없는 정적 배포에서는 안내만 보인다. 키는 서버 설정 파일의 `ai` 블록에만 저장되고 화면에는 존재 여부(저장됨/환경 변수/없음)만 돌아온다. 저장된 키가 있으면 그것을, 없으면 OPENAI_* 환경 변수를 쓴다(환경 변수는 OpenAI만). 실시간 음성과 화면 값 변경 도구는 OpenAI 전용이고 다른 프로바이더는 텍스트 대화만 한다. 운영자 지시어는 기본 HATCHERY 지시문 뒤에 "운영자 추가 지시"로 이어 붙는다. 영향 경로: aiConfig.ts(모델·검증·마스킹) → feedConfig.ts(HatcheryConfig.ai) / hatcheryConfig.ts(키 병합) → server/cinema/aiProviders.ts(런타임 해석·프로바이더 요청·접속 테스트) → server/cinema/openai.ts(지시문·응답) → api/cinema/admin/ai(+/test) → admin/HatcheryAiSettings.tsx → app/cinema/ai/page.tsx → FilmDock.tsx(links.ai). 검증: cinemaAiConfig.test.ts, cinemaAiServer.test.ts, cinemaOpenAi.test.ts.
- 메뉴 관리 큐브는 메인 화면(preview)에서만 렌더한다. 장면 화면의 왼쪽 위는 각 장면의 제목·판독 영역이라 큐브가 가리면 안 된다(FilmDock.tsx의 camera.preview 조건).
- 큐브 메뉴 펼침: 큐브(버튼 `메뉴 관리`, aria-haspopup=menu)를 클릭하면 전개도처럼 큐브 자리를 기준으로 오른쪽으로 세 면(관리·AI·음성), 아래로 세 면(피드·화면·설정)이 연달아 펼쳐진다(타일 1.15×모서리, 간격 10px). 큐브 자리에는 `접기` 타일이 나타나 다시 큐브로 복원한다. 각 타일은 큐브 중심에서 그 면의 회전 자세(rotateY ±90°/180°, rotateX ±90°)로 시작해 순서대로 70ms 시차를 두고 날아와 평평하게 눕고, 본체는 사라진다. 닫을 때는 역순 시차로 큐브로 접힌다. 전개도 원점(큐브 슬롯)은 큐브 중심에 맞추고, 팔이 화면 밖으로 나가면 16px 여백 안으로 당긴다. Escape·바깥 클릭·타일 선택으로 닫히고 초점은 큐브 버튼으로 돌아간다. 방향키 오른쪽/아래는 접기 → 오른쪽 팔 → 아래 팔 순으로, 왼쪽/위는 반대로 순환한다. 열려 있는 동안 쇼케이스 회전은 멈춘다. 관리 타일은 next/link로 /cinema/admin에 이동하고(FilmDock의 links), 나머지는 FilmDock의 onSelect에서 연결한다. 계산: filmMenuCube.ts(cubeMenuSlots·cubeMenuTileSize·cubeMenuOrigin) → FilmMenuCubeView.tsx(open 상태·초점·키보드) → filmMenuCube.module.css(.menu/.panel 전환) → FilmDock.tsx(onSelect). 검증: cinemaMenuCube.test.ts, cinemaMenuCubeMarkup.test.ts.
- 큐브 외형·동작: 검은 플라스틱 틈과 면당 3×3 스티커(빨강·파랑·초록·주황·흰·노랑)로 맞춘 퍼즐 큐브이며, 가운데 스티커의 관리·AI·음성·피드·화면·설정 아이콘은 호버·키보드 초점에만 나타난다. 마우스를 1초 올려 두면 맞춘 큐브가 층을 돌려 섞인 뒤, 같은 층 회전으로 다시 맞추고, 떠나면 남은 회전을 되감아 맞춘 상태로 돌아온다. 아래 안내 문구는 두지 않는다. 모서리 길이는 데스크톱 86px, 680px 이하 70px, 높이 500px 이하 58px이며(원래 108·88·72의 80%), 화면이 더 작으면 16px 가장자리·라벨·4px 부유 폭까지 화면 안에 남도록 축소한다. 약 7px 이하 짧은 클릭과 키보드 초점은 이후 관리 메뉴 연결을 위한 자리이며 지금은 장면을 바꾸지 않는다. 포커스 링만 드라큘라 핑크를 쓰고 스티커 색은 바꾸지 않는다. 라우팅은 아직 붙이지 않는다. 영향 경로: filmMenuCube.ts → FilmMenuCubeView.tsx / filmMenuCube.module.css → FilmDock.tsx. 검증: cinemaMenuCube.test.ts, cinemaMenuCubeMarkup.test.ts 및 실제 왼쪽 상단 도킹·호버 아이콘 확인.
- 하단 링은 열린 상태에서 일반 높이 144px, 높이 500px 이하 124px다. 접히면 구체는 화면 전면의 뷰포트 오버레이로 떠 있으며, 최초 진입과 매번 축소할 때 오른쪽 하단 구석으로 모인다. 지름은 데스크톱 240px, 680px 이하 180px, 높이 500px 이하 132px 우선이며, 화면이 더 작으면 16px 가장자리·라벨·4px 부유 폭까지 화면 안에 남도록 축소한다. 마우스/터치로 구체를 직접 밀면 포인터를 따라 이동하고, 다음 축소 전까지 놓은 위치를 유지한다. 다시 축소하면 이전 드래그 위치 대신 현재 화면의 오른쪽 하단으로 돌아간다. 드래그를 놓으면 짧게 감속한 뒤 그 자리에서 4px 부유와 30초 회전을 계속하며, 약 7px 이하 짧은 클릭과 키보드 Enter/Space만 메뉴를 연다. 동작 줄이기에서는 드래그와 즉시 변형만 유지하고 관성·부유·회전을 끈다. 닫힌 nav는 구체 버튼 외 포인터를 막지 않고 도크 예약은 `calc(32px + var(--film-safe-extra))`만 사용하며, 열린 상태 208px/낮은 화면 172px는 유지한다. 영향 경로: filmMenuGlobe.ts(크기·휴지 위치·클램프·제스처 계산) → useFilmMenuGlobe.ts / FilmMenuGlobeView.tsx / filmMenuGlobe.module.css(위치·입력·표현) → FilmChapterMenu.tsx / filmMenuRing.module.css → filmDock.module.css / film.module.css(열림 링·닫힘 예약) → jarvis.module.css / useFilmPlayback.ts / EnvironmentZoneInteraction.tsx / SmtFactoryExplorer.tsx(공유 viewport). 검증: cinemaMenuGlobe.test.ts, cinemaMenuGlobeMarkup.test.ts, cinemaPersistentDock.test.ts 및 실제 마우스·키보드·축소 화면 확인(실기기 터치는 별도 검증). 성능 규칙: 링이 열린 동안의 turn 변경(드래그)은 useFilmMenuGlobe.ts에서 링 타깃을 재측정하지 않는다(타일 16개 getComputedStyle·offsetTop 읽기가 pointermove마다 강제 레이아웃을 일으켜 드래그가 끊겼음). 재측정은 변형 중과 열림/접힘 전환 시에만 한다. 접힌 구체는 4초 동안 마우스·초점·드래그가 없으면 1.4초에 걸쳐 50% 크기로 줄고(부드러운 easing), 마우스를 올리거나 초점·드래그가 오면 0.35초 만에 원래 크기로 돌아온다. 동작 줄이기에서는 줄어들지 않는다. 축소·복원의 기준점은 구체의 오른쪽 아래 접점이다(휴지 위치에선 화면 오른쪽 하단 구석): 반지름 변화만큼 중심을 옮겨 접점이 고정되고, 클램프는 현재 표시 지름을 쓴다. 누른 채 잡고 있으면(드래그 포함) 프레임 루프가 계속 돌며 원래 크기로 자라고, pointermove마다가 아니라 프레임마다 한 번만 다시 그린다. 유휴 시간은 벽시계 기준이라 느린 프레임이 축소를 늦추지 않는다. 축구공 캔버스는 항상 전체 지름으로 래스터하고 면 타일과 같은 scale로 함께 줄여 두 층이 어긋나지 않는다. 래스터는 크기별로 디스크 마스크·위도 행·조명을 미리 계산해 두고 프레임에는 경도 이동과 샘플링만 한다(격자는 텍스처 알파에 있다). 계산: filmMenuGlobe.ts(globeRestStep·globeRestScale·drawSoccerSphere) → useFilmMenuGlobe.ts(rest·idle·awake·dragDirty, 버튼·공 scale과 면 반지름). 검증: cinemaMenuGlobe.test.ts, Playwright로 축소·호버·누름 유지·드래그 확인.

- 상단 8개 지표 카드의 높이는 이전 대비 약 70%로 압축한다. 카드 폭·한 줄 가로 스크롤·8개 수치와 제목/설명은 유지하고 세로 패딩 및 계기 SVG 높이를 축소한다. 데스크톱 계기 24px, 모바일 16px이며 수치 글자는 최대 32px/모바일 20px로 유지한다. 전체 scaleY로 글자를 찌그러뜨리거나 고정 높이로 내용을 자르지 않는다. 영향 경로: jarvisMetricCards.module.css. 검증: 실제 카드 높이와 텍스트 영역, 기존 cinemaMetricStrip.test.ts.

- HATCHERY 로고는 메인 헤더의 첫 행 상단 중앙에 독립 배치한다. 상단 나침반·방위각 눈금·방향·수치 표시는 제거하고 JarvisHeading을 마운트하지 않아 해당 애니메이션도 실행하지 않는다. START/STOP은 좌측 음성 제어 영역에 유지한다. 모바일도 동일하며 로고 SVG의 compact viewBox로 여백만 줄이고 네온 연출을 유지한다. 영향 경로: JarvisMainHeader.tsx → JarvisIdentity.tsx / jarvisHeader.module.css. 기존 로고 아래 START 결합 및 상단 방위각 표시 규칙을 대체한다. 검증: cinemaHeaderIgnition.test.ts.

- 상단 지표는 8개(생산 진행·공정 병목·SPC 이탈·사용 전력·에너지 효율·검사 합격·평균 온도·평균 습도)를 모든 화면에서 한 줄 가로 스크롤로 표시한다. 추가 수치는 기존 에너지/제품검사/10개 ZONE 시연 데이터에서 계산하며 평균값의 정상 여부가 아니라 개별 구역의 온도/습도 이탈을 경고로 표시한다. 항목별 kind·값·단위·note가 단일 정의에 묶인다. 자동 이동은 20px/s, 양 끝에서 1.8초 대기 후 역방향이며 DOM 복제 없음. 마우스/키보드 초점·직접 조작·페이지 숨김·동작 줄이기에서는 멈추고 정지/재개 버튼을 제공한다. 좌측 로고/START와 우측 카메라는 이동하지 않는다. 이전 4카드/모바일 스냅 규칙을 대체한다. 영향 경로: jarvisMainData.ts → JarvisMetricCards.tsx / JarvisMetricInstruments.tsx → useMetricStripScroll.ts / jarvisMetricCards.module.css. 테스트: cinemaMetricStrip.test.ts, cinemaHeaderIgnition.test.ts.

- START/STOP 음성 제어는 중앙 파동 영역에서 좌측 상단 HATCHERY 로고 아래로 이동한다. JarvisMain은 기존 음성·카메라 시작/종료 콜백을 보유하고 JarvisMainHeader의 ignition 슬롯에 단일 제어를 전달한다. 좁은 로고 열에서는 장식 계기를 숨긴 compact 표현으로 상태·응답 중지 버튼을 유지한다. 모바일(680px 이하)도 로고와 START를 숨기지 않으며 카메라와 같은 행, 주요 지표는 다음 행의 한 줄 가로 스크롤로 배치한다. 중앙은 제목·파동·답변·입력의 4행으로 구성한다. 영향 경로: JarvisMain.tsx → JarvisMainHeader.tsx / jarvisHeader.module.css → JarvisIgnition.tsx / jarvisIgnition.module.css; 중앙 행은 jarvis.module.css. 검증: cinemaHeaderIgnition.test.ts 및 실제 화면 위치 확인. 페이지 진입만으로 마이크·카메라를 시작하지 않는 규칙은 유지한다.

- 하단 장면 메뉴는 16개 육각 아이콘을 기울어진 3D 링 둘레에 배치한다. 원근 깊이·크기·밝기·좌우 기울기를 공통 회전 슬롯에서 계산한다. 드래그/스와이프·좌우 버튼·방향키로 탐색하고 놓으면 가까운 메뉴에 정렬한다. 비정면 첫 클릭은 정렬만, 정면 클릭/Enter/Space는 실행한다. 실제 재생 메뉴의 aria-current와 탐색 정면 표시는 구분하며 드래그 취소는 실행하지 않는다. 키보드 초점과 동작 줄이기를 지원한다. 메인/장면/모바일은 같은 개방·접힘 계약을 사용하고 재개방 시 폭을 재측정한다. 닫힘/변형 중에는 링 조작부만 inert가 되며 링과 같은 nav 안의 형제인 구체는 계속 조작한다. 기존 격자 메뉴와 화면별로 달랐던 개방 규칙을 대체하며 색·아이콘·테마는 재사용한다. 영향 경로: filmMenuRing.ts(순환/3D 좌표) → useFilmMenuRing.ts(입력/스냅/크기) → FilmChapterMenu.tsx / useFilmMenuGlobe.ts / FilmMenuGlobeView.tsx / filmMenuRing.module.css / filmMenuGlobe.module.css → FilmDock.tsx(선택·접힘) / film.module.css(여백). 테스트: cinemaMenuRing.test.ts, cinemaMenuGlobe.test.ts, cinemaMenuGlobeMarkup.test.ts 및 실제 브라우저 조작. 화살표는 링 좌우 끝, 선택 이름과 실행 안내는 중앙 빈 공간에 놓고 별도 하단 안내 줄은 제거한다. 전체 키보드 안내는 접근성 설명으로 유지한다. 링 앞뒤 간격은 110px이며 모바일에서도 버튼 터치 영역 44px을 유지한다.

- 제품 표시 이름은 `HATCHERY`다. 화면·접근성 라벨·브라우저 제목·로고·시작 음성·로컬 및 AI 자기소개에 동일하게 적용한다. 기존 `Jarvis*` 파일/심볼과 `jarvis` 저장 설정 키는 호환성을 위해 유지한다. 이름 변경 영향 경로: src/app/layout.tsx → SignalFilm.tsx / FilmDock.tsx / JarvisMain.tsx / JarvisIdentity.tsx / JarvisIgnition.tsx / JarvisDialogue.tsx / JarvisConversationTrail.tsx → jarvisStartupSound.ts / jarvisAudio.ts / jarvisCommands.ts / useJarvisLocalVoice.ts / robotVoice.ts → src/server/cinema/openai.ts.

- 모바일 상단 주요 지표(680px 이하)는 생산·공정·품질·전력 카드를 한 줄의 가로 스크롤로 표시한다. 카드 너비는 지표 영역의 82%(최소 140px)로 설정해 다음 카드 일부를 노출하며, 왼쪽으로 밀면 오른쪽의 다음 지표가 나타난다. 카드 단위 스냅과 얇은 스크롤바를 사용하고 키보드 초점·방향키 스크롤을 지원한다. 우측 카메라 위치는 유지한다. 자동 이동 없이 사용자가 읽는 속도로 넘긴다. 영향 경로: JarvisMetricCards.tsx → jarvisMetricCards.module.css(한 줄·너비·스크롤) / jarvisHeader.module.css(상위 2열/4열 강제 배치 해제).

- 온습도 ZONE 객체 선택 1단계: 카드의 기존 ZONE ID를 사용해 수동 선택한다. 카드 클릭·방향키로 선택하고 빈 공간 클릭·Esc로 해제한다. 수동 선택은 자동 센서 순회보다 우선하여 카드 외곽·접점과 중앙 계기 값·연결선을 갱신한다. 재생은 계속되며 하단 재생·정지 버튼을 사용한다. 중앙 계기의 기존 2.5~21.5초 표시 시간과 이후 그래프·히트맵 전환은 유지하므로 계기가 없는 구간에서는 카드 선택 강조만 표시한다. 35초 히트맵 시작, 선택 카드가 보이지 않는 시점으로 탐색, 다른 장면·메인 메뉴 진입, 재시작 시 선택을 해제한다. 실제 Canvas에 그린 상태의 부유·배율·기울기·절단 모서리를 클릭 판정과 공유하고 겹친 카드는 마지막 그리기 순서 우선으로 선택한다. 카드 값과 관리 범위는 수정하지 않는다. 영향 경로: zoneEnvironment.ts(선택 우선순위·가시성) → environmentLayout.ts(윤곽·그리기 순서) → environmentSceneObjects.ts(객체 ID·경계·선택·CSS/DPR 좌표) → environmentSelection.ts / useEnvironmentSelection.ts(선택 세션) → useFilmPlayback.ts → drawSignalFilm.ts / drawWaveFilm.ts → components/drawZoneEnvironment.ts. 입력 UI는 SignalFilm.tsx → EnvironmentZoneInteraction.tsx / environmentInteraction.module.css. 검증: cinemaEnvironmentSelection.test.ts, cinemaFrameState.test.ts.

- 화면 문구는 지표명·수치·단위·기준·상태·데이터 출처·조작 안내만 사용한다. 광고 카피, 비유적 제목, 숫자·차트·카메라가 움직이는 방식을 설명하는 상하단 자막은 넣지 않는다. 장면 메뉴명은 유지하고 설정은 장면명, 접근성 설명은 표시 데이터 항목을 짧게 나열한다.

- 투명 설비 분석(machine)은 사용자 제공 AEROX 레이싱카 이미지의 구성을 참고한 투명 포뮬러 차량이다. 중앙 사선 차량에 모노코크·헤일로·배터리·V형 엔진 뱅크·라디에이터·서스펜션·4개 바퀴와 브레이크·전후 윙을 개별 Canvas 도형으로 그린다. 청록색 유체 흐름과 앰버 열 흐름이 차량을 따라 이동한다. 좌측 ERS/냉각·공력 부하·다운포스, 우측 파워유닛·댐퍼·브레이크 6개 패널을 둔다. 모든 값은 시연 스냅샷이며 실제 차량 측정이나 CFD 결과가 아니다. 사진을 그대로 배치하지 않고 구조를 자체 도형으로 재구성한다. 36초 동안 공력→ERS→파워유닛→서스펜션→브레이크를 강조하며 공통 시간으로 재생·정지·탐색을 재현한다. 영향 경로: raceCar.ts(상태·투영·시연값) → components/drawRaceCar.ts / components/drawRaceDiagnostics.ts → drawTransparentMachineFilm.ts → filmProgram.ts; 음성 명령 jarvisCommands.ts. 이전 핸드폰 부품 모델은 보존하되 현재 machine 장면에서는 사용하지 않는다. 검증: cinemaRaceCar.test.ts.

- 바이저 평면은 PCB 로더 → 스크린프린터 → SPI → 마운터 → MAOI → 리플로우 → AOI → 언로더 8대를 한 번씩 배치한다. `smtLine.ts`가 공정 순서·연출용 크기·위치·열원 대상(06 리플로우)의 단일 기준이다. 전체 보기에서 모두 보이고 확대 시 장면과 냉각팬 추적점을 같은 변환으로 이동한다. 로더/언로더는 매거진과 승강부, 프린터는 인쇄 후드·스퀴지, SPI는 측정 헤드, 마운터는 노즐 헤드·피더 릴, MAOI는 마운터 직후의 자동 광학검사기로 검사창·카메라 헤드를 사용, 리플로우는 긴 분할 터널·배기·냉각팬, AOI는 광학 검사창과 다각도 헤드로 구분한다. MAOI는 실장 후·리플로우 전 검사이며, 뒤쪽 AOI는 리플로우 후 검사다. 설비 크기는 연출용이며 실제 치수가 아니다. 온도는 리플로우 냉각부 시연값으로 표시하며 납땜 피크 온도로 해석하지 않는다. 영향 경로: smtLine.ts → components/drawSmtEquipment.ts / components/drawInspectionPanorama.ts → drawPlanarVisorFilm.ts; components/drawVisorOverlay.ts는 해당 장면만 8개 접점·06 대상으로 표시한다. 검증: cinemaSmtLine.test.ts.
- 설비 외형 참고 자료(2026-09-07): [ASYS 매거진 로더·언로더](https://www.asys-group.com/en/smt-solutions/handling/pcb-handling/line-loading-line-unloading), [Yamaha 프린터·마운터 제품군](https://global.yamaha-motor.com/business/smt/lineup/), [Koh Young SPI·AOI](https://kohyoung.com/en/solutions/), [Heller 1936 MK7 리플로우](https://hellerindustries.com/1936-reflow-oven/). 제품 사진을 배포 파일에 넣지 않고 구조적 특징을 자체 Canvas 도형으로 재구성한다.

- 정보창 연출은 사용자 제공 4종 HUD 참고 이미지에 맞춰 청록색(#00f5ed) 단선 외곽·비대칭 절단 모서리·굵은 탭·사선 해칭을 사용한다. 정보 콘솔의 설비/분석에는 command/analysis, 바이저 열원에는 sensor, 평면 바이저에는 telemetry 프레임을 적용한다. 상하 장식 깊이는 큰 창에서도 제한하여 제목과 수치를 침범하지 않는다. 기존 시간 기반 외곽선 생성·타이핑·포커스·경고 텍스트를 유지한다. 영향 경로: components/infoPanelFrame.ts → components/drawInfoPanel.ts → drawConsoleFilm.ts / components/drawVisorReadout.ts / drawPlanarVisorFilm.ts.

## Cinema prototype (/cinema)
- 대화 제어는 `JarvisIgnition.tsx` / `jarvisIgnition.module.css`의 원형 START/STOP 시동 버튼이다. 금속 테두리·오목한 중심·전원등과 외곽 회전 아크를 사용하고 옆에 회전 링·움직이는 바늘의 보조 게이지를 둔다. 게이지는 수치 측정값이 아닌 장식이며 실제 연결·응답 상태를 별도 문구와 회전 속도로 표시한다. 기존 시작/종료/응답 중지 이벤트와 기동 음성을 유지한다. 낮거나 좁은 화면에서는 버튼을 축소하고 동작 줄이기 설정은 회전을 정지한다. 영향 경로: JarvisMain.tsx → JarvisIgnition.tsx / jarvisIgnition.module.css.
- 중앙 파동 뒤의 `JarvisConversationTrail.tsx`는 현재 메시지를 제외한 이전 대화 최대 12개를 희미한 원근 텍스트로 위쪽 순환 이동시킨다. 실제 대화만 사용하며 파동 영역 안에서 클리핑한다. 배경 기록 z-index 0 → 중앙 감쇠막 1 → 파동 Canvas 2로 분리하고 포인터 이벤트를 차단하여 버튼·현재 답변·입력 영역을 침범하지 않는다. 동작 줄이기에서는 정지한다. 영향 경로: JarvisMain.tsx → JarvisConversationTrail.tsx / jarvisConversationTrail.module.css → jarvis.module.css.
- 대화 시작은 `jarvisStartupSound.ts`의 약 1.4초 모터 상승음·전자 확인음 뒤 “HATCHERY initializing.”을 영어로 안내한다. Realtime은 연결 후 한 번의 response.create로 선택된 음색·로봇 효과를 사용하며, 안내 종료까지 마이크 전송을 막아 자기 음성을 질문으로 인식하지 않게 한다. 시작 취소·화면 이탈·오류 시 발진기와 연결을 해제한다. AI 미설정 시 기존 브라우저 음성 경로에서 영어 안내 후 인식을 시작한다. 영향 경로: jarvisRealtimeSession.ts / useJarvisLocalVoice.ts → jarvisStartupSound.ts.
- 좌측 상단 브랜드는 `JarvisIdentity.tsx` / `jarvisIdentity.module.css`의 핑크 네온 HATCHERY 로고다. 12초 주기로 파동 → 수평선 → 글자 형성·유지 → 파동 복귀를 반복하고, 동작 줄이기 환경에서는 글자만 정적으로 표시한다. 기존 브랜드 아래 MANUFACTURING / ASSISTANT 및 메인 메뉴 설명은 제거한다. 이 로고만 고정 핑크 색을 사용하며 주변 HUD 테마는 유지한다.
- 우측 상단 카메라의 연결/끄기/취소 버튼은 영상 프레임 안 우하단에 겹쳐 배치한다. 외부 버튼 행을 없애고 영상 폭을 120px에서 160px로 확대하며 좁은 화면에서는 해당 고정 영역의 폭에 맞춘다. 상태는 영상 안 좌상단에 둔다. 영향 경로: JarvisMainHeader.tsx → jarvisHeader.module.css.
- 목소리 선택: 좌측 `JarvisAiVoiceSettings.tsx`(Realtime)와 `JarvisVoiceSettings.tsx`(브라우저)는 공통 `JarvisVoiceGenderToggle.tsx`로 남성/여성만 고른다(jarvisVoiceGender.ts: 남성=cedar·낮은 음높이 0.72, 여성=marin·음높이 1.0). 스타일 목록과 CEDAR/MARIN/ASH/ECHO 음색 선택은 화면에서 없앴다. 로봇 효과(`robotVoice.ts`)는 기본값 안드로이드형 75%로 고정 적용되며 Realtime 목소리는 세션 종료 후 바꾼다. `robotVoice.ts`가 수신 음성에 필터·링 변조·포화·출력 압축을 적용하고, 단일 Web Audio 출력으로 재생한다. 원본 HTMLAudioElement는 음소거하여 이중 재생을 막고, 처리 후 AnalyserNode를 중앙 파형에 전달한다. 중지 시 발진기·노드·AudioContext를 해제하며 마이크 입력은 가공하지 않는다. 영향 경로: `useJarvisVoice.ts` → `jarvisRealtimeSession.ts` → `robotVoice.ts`; UI `JarvisMain.tsx` → `JarvisAiVoiceSettings.tsx` → `jarvis.module.css`.
- 상단 4개 정보 카드는 사용자가 제공한 첫 번째 HUD 프레임을 참고한 동일한 사선 모서리·이중 외곽선·상하 포인트 바·좌하 분절 표시를 사용한다. `JarvisMetricFrame.tsx`의 장식 SVG를 함께 재사용하며 선 굵기는 vector-effect로 유지한다. 테두리는 현재 테마 색을 따르고 경고 상태도 외곽선 구조를 바꾸지 않는다. 배경은 같은 다각형으로 잘라 사각 잔상이 남지 않게 한다. 영향 경로: JarvisMetricCards.tsx → JarvisMetricFrame.tsx → jarvisMetricCards.module.css.
- 상단 4개 지표는 JarvisMetricCards.tsx와 jarvisMetricCards.module.css에서 서로 다른 계기로 표현한다. 생산은 실제 달성률의 입체 분절 바와 컨베이어 광선, 공정은 6개 노드의 흐름과 실제 병목 위치 맥동, 품질은 부분군 평균·관리한계·이탈 점과 이동 주사선, 전력은 실제 부하율의 반원 게이지·고정 바늘과 장식 전류 파형이다. 작은 부유·광선은 CSS로 구동하며 값이나 측정 파형 자체를 임의 변화시키지 않는다. hover 또는 동작 줄이기 설정에서 장식 움직임을 멈춘다. 기존 jarvisMainData 스냅샷과 중앙 무스크롤·우측 카메라 고정·최상단 방위각 구조를 유지한다.
- 방위각·나침반은 JarvisMainHeader의 첫 행 전체를 차지하는 독립 가로 영역에 배치한다. 그 아래 두 번째 행에 요약 정보와 우측 고정 카메라를 둔다. 상단 전체 높이를 줄여 중앙의 스크롤 없는 입력 영역을 유지한다.
- 중앙 대화 영역은 스크롤 없이 상태 제목·가변 높이 음성 파동·대화 제어·응답·입력창을 grid로 배치한다. 5개 보조 원형 게이지는 우측 스트림의 2열 CHANNELS 영역, 목소리 설정·바로가기·최근 질문·연결 안내는 좌측으로 이동했다. 중앙 그리드는 상단 제어·축소된 리액터(최대 0.62fr)·답변(최소 7.2em·0.38fr)·입력창이다. JarvisDialogue.tsx는 긴 응답을 200개 Unicode 코드 포인트씩 이전/다음 페이지로 표시하며 새 응답에서 첫 페이지로 복귀한다. 답변 본문 최소 높이는 6.4em(약 4줄)이다. 내용이 길어져도 중앙 스크롤을 생성하거나 답변을 버리지 않는다. 영향 경로: JarvisMain.tsx → JarvisDialogue.tsx / JarvisChannelDials.tsx → jarvis.module.css / jarvisStream.module.css.
- 메인 상단은 JarvisMainHeader.tsx의 요약·방위각·우측 상단 카메라 고정 영역이다. JarvisMain.tsx는 고정 header와 스크롤 body를 분리하며 JarvisCamera는 정보 스트림의 자식으로 배치하지 않는다. 좁은 화면에서도 header를 유지하고 body만 세로로 스크롤한다. JarvisHeading.tsx는 옅은 원근 격자 위에서 나침반 바늘과 수평 눈금을 같은 시연 각도로 구동하고 방위각 수치를 표시한다. 실제 장치 센서와 무관한 DEMO이며, 동작 줄이기 설정에서는 90도로 멈추고 해제 시 RAF를 정리한다. 영향 경로는 jarvisHeader.module.css / jarvis.module.css / FilmCameraControls.tsx의 안내 문구다.
- HATCHERY 음성 출력: OpenAI 설정 시 useJarvisVoice.ts가 Realtime WebRTC 세션을 선택한다. 좌측 설정의 남성/여성 선택이 cedar/marin 음색과 프롬프트 페르소나를 함께 정하며 활성 세션 중 변경은 막는다. AI 생성 음성·외부 전송·사용량 과금을 표시한다. 실제 마이크/응답 오디오의 AnalyserNode가 중앙 파형을 구동한다. 미설정 상태에서는 useJarvisLocalVoice.ts와 jarvisSpeechProfile.ts가 브라우저 한국어 TTS를 담당하며, 남성은 알려진 남성 음성 이름(InJoon 등)·pitch 0.72, 여성은 알려진 여성 음성 이름(SunHi 등)·pitch 1.0을 고르고 없으면 반대 성별로 확인되지 않은 한국어 음성으로 대체한다. 설정된 OpenAI 연결 실패를 브라우저 음성으로 조용히 대체하지 않는다.
- Magnific의 HUD 부품 모음(https://www.magnific.com/free-vector/futuristic-user-interface-elements-vector-set_10601013.htm)을 시각적으로 참고해 절단 링·분할 눈금·비대칭 정보창을 자체 SVG/Canvas 도형으로 재구성했다. 원본 이미지 파일을 사용하지 않는다. JarvisHoloGauge.tsx는 반사 테두리와 7px 뒤쪽 링으로 두께를 표현하고, JarvisMiniDial의 arcs/sectors/vernier/double/blocks 옵션은 실제 비율의 값 아크와 장식 회전을 분리한다. JarvisWave.tsx → drawJarvisVoiceField.ts는 같은 원근 안에서 13개 리본 면·교차선·이동광을 그리며 listening 상태에서만 실제 마이크 샘플을 반영한다. useFilmPlayback.ts → drawJarvisBackdrop.ts는 메인 전용 어두운 공간 배경을 그린다. jarvis.module.css와 jarvisStream.module.css가 중앙 파동 크기와 절단형 정보창을 담당한다.
- 메인 정보 스트림은 참고 HUD처럼 투명한 다중 테두리·주사선·분절 원형 계기를 사용한다. JarvisStream.tsx가 각 section의 변형 전 위치에서 읽기 지점과의 거리를 계산하고 −80~25px 깊이와 좌우 대칭 안쪽 기울기를 카드 전체에 적용한다. 수치·계기·버튼은 같은 면을 따른다. JarvisHoloGauge.tsx는 실제 비율의 40분절 게이지와 장식용 반대 회전 링을 분리하고, JarvisChannelDials.tsx는 우측 정보 스트림에 생산·공정·SPC·전력·검사 보조 계기를 배치한다. JarvisOperations.tsx는 공정 처리능력을 공통 700 EA/h 눈금의 발광 채널로 표시하며 품질 그래프는 실제 부분군 평균과 이탈 점을 그린다. 주사선·장식 회전은 읽기 중 정지하고 동작 줄이기 설정에서는 원근 및 자동 회전을 끈다. 스타일은 jarvisStream.module.css다.
- 온습도 ZONE 카드는 위 ZONE 01~05·아래 ZONE 06~10을 5개씩 고정 배치한다. 기준 anchor는 x=200+220×열, y=185/580이며 각기 다른 6.2~7.55초 주기로 상하 5·좌우 2 논리 좌표, 깊이 4와 작은 기울기를 더해 부유한다. 선택 카드의 확대는 최대 1.07배로 제한하고 흔들림을 줄인다. 중앙은 온도(430,390)·습도(850,390), 반지름 132의 투명 원형 계기 두 개를 고정한다. 첫 선택 때 외곽 분할 링·눈금·수치 아크를 순서대로 조립하고 이후 센서 선택에서는 계기를 확대·축소하지 않는다. 바깥 분할 링과 안쪽 장식 눈금은 서로 반대 방향으로 회전하며 센서 교대 때 연속적으로 회전 가속을 더한다. 측정 눈금·값 아크·관리 범위·문자는 고정 방향으로 읽는다. environmentLayout.ts의 연결선은 카드의 실제 배율·기울기를 반영해 가장 가까운 원형 계기의 위/아래 테두리에 닿고, 중앙 두 계기도 짧은 연결선으로 이어진다. 밝은 중심선·부드러운 외곽·양 끝 접점·이동광을 유지하고 공통 장면 시간으로 일시정지·배속·역방향 탐색을 재현한다.
- 독립된 연출 비교 화면. 온습도 모니터링(52초) → 기어 연동(28초) → 설비 스캔(22초) → 지표 펼침(32초) → 변화 추적(24초) → 정보 콘솔(28초) → 바이저 3D(48초) → 바이저 평면(32초) → 막대 비교(28초) → 파이 구성(28초) → 코너 전개(40초) → 투명 설비 분석(36초) → 살아 있는 공정망(32초) → 에너지 파동(32초) → 제품 내부 검사(36초) → SPC 분석(40초)을 총 538초 순서로 반복한다. 데이터는 시뮬레이션이며 실제 SSE 연동은 아직 적용하지 않는다.
- 하나의 Canvas 2D 장면과 시간축에서 선의 형태를 연속 변형하고, 정보 연결선은 같은 형태에서 뻗어 나온다.
- 장면의 페이드·합성 상태는 프레임 밖으로 전달하지 않는다. drawTransparentMachineFilm.ts의 주석선은 자체 save/restore로 감싸고, drawSignalFilm.ts는 매 프레임 alpha 1·source-over·필터/그림자 없음으로 시작한 뒤 호출자 상태를 복원한다. 장면 전환 암전과 하단 표기도 객체의 투명도와 독립적으로 그린다. 투명 설비 종료의 alpha가 다음 장면 배경을 지우지 못하게 해 filmTexture.ts의 유리 반사·bloom이 누적되던 문제를 방지한다. 검증 경로: tests/unit/scenes/cinemaFrameState.test.ts의 실제 장면 전환·페이드·그리기 상태 회귀 검사.
- 포커스 연출은 대상 탐색 → 깊이 방향 접근/zoom in → 정보 판독 → 후퇴/zoom out → 전체 흐름 복귀를 기본으로 한다. 색·선의 강조만으로 포커스를 대신하지 않으며, 장면 암전 전에 축소와 복귀가 실제로 보여야 한다. 평면 장면에도 전경과 배경의 깊이 차이를 둔다. 예외로 온습도 중앙 계기는 사용자 요청에 따라 고정 크기의 원형 링 조립·외곽 회전·센서 연결 교대로 포커스를 표현하고, 마지막에 투명해지며 그래프에 자리를 넘긴다.
- 초점의 공용 경로는 filmFocus.ts의 focusEnvelope(접근·유지·복귀 시간) → focusProjection(전진 깊이의 원근 투영) → 부품 및 연결점 투영이다. 자체 타이머 없이 장면 시간을 사용해 정지·역방향 탐색·배속에서 같은 좌표를 복원한다. 전체 화면에 일괄 배율을 적용하지 않고 대상의 깊이를 바꾸며 제목과 주변 광학 계기는 화면 좌표를 유지한다.
- 스캔·지표·사건은 초점을 받은 경로와 부착 라벨이 함께 전진한다. 온습도 모니터링은 위아래의 작은 ZONE 카드와 중앙 원형 온도·습도 계기 크기를 유지하며 선택 구역의 값·연결선을 갱신한다. 맞물린 기어는 접점을 유지하는 하나의 깊이 평면으로 접근하고 테두리 옆면을 드러낸다. 정보 콘솔은 코어 → 설비 정보창 → 분석창 → 동기 회전 순서로 초점을 옮기며, drawInfoPanel.ts의 focus 입력과 infoPanelFocusProjection이 정보창과 연결선을 같은 위치에 둔다.
- 온습도 모니터링(wave)은 52초다. 0~2.5초에 전체 구역을 표시하고 2.5~21.5초에 ZONE 01~10을 빠짐없이 각 1.9초씩 선택한다. 중앙 원형 계기는 2.5~3.4초에 조립된 뒤 계속 유지하며, 선택 센서 값과 연결선만 교대하고 마지막 0.65초에 사라진다. 위아래 카드 위치를 유지하며 23~26.14초에 안쪽의 24시간 그래프 10개를 순차 전개하고, 카드와 그래프는 34~35.5초에 함께 사라진다. 35~38초에는 상공에서 수직으로 내려다본 공장 열화상을 보여주고, 38~42초에 라인 입구로 하강한 뒤 42~46초에 설비 사이로 전진한다. 46~50초에는 다른 라인 쪽으로 선회하고 50~52초에 상공으로 오르며 사라진다. 표시 단계는 ENVIRONMENT_TIMING, 비행 경로는 environmentHeatmapProjection.ts가 같은 장면 시간으로 계산한다. 상단 장면 제목은 단계별로 한 개만 표시하고 히트맵에서는 온도 값만으로 정상·이탈·미확인 수를 집계한다. 센서의 전달값은 연출에 따라 바꾸지 않고 기본 데이터는 시연값임을 명시한다.
- 마지막 온도 히트맵은 기존 바이저 3D의 5개 SMT 라인·40대 설비를 그대로 사용하는 공장 공간이다. 별도 흰색 벽체나 방 모형을 추가하지 않는다. components/drawSmtFactory.ts와 factoryProject가 동일한 cameraOverride를 공유해 설비·열화상 바닥·센서 위치에 실제 원근을 적용한다. drawSmtFactory의 ground 옵션은 기본 표시 상태를 유지하며, 온습도 공간에서만 ground: false를 전달해 열화상 바닥 위에 설비를 그린다. 카메라는 처음에 모든 센서가 보이는 수직 상공 시점에 머물다가 설비 높이보다 높은 경로로 하강·전진·선회한다. 위치는 구간별로 부드럽게 보간하고 방향은 끝 시점의 yaw를 최단 각도로, pitch를 직접 보간한다. 선회 중 바라보는 점이 카메라 바로 아래로 접근해 다시 수직 하향으로 꺾이는 현상을 방지한다. 비행 중 화면 밖이나 카메라 뒤로 나간 센서의 라벨은 표시하지 않는다.
- 열화상은 10개 센서 지점 사이를 거리의 세제곱 역수(IDW, power 3)로 보간하며 색이 청→녹→황→적 순으로 부드럽게 번진다. 센서 위치에서는 원래 온도를 그대로 보존하고 보간값도 유효 센서의 최솟값~최댓값 안에 둔다. 구역별 관리 범위와 별개로 모든 공간에 같은 절대 온도 °C 범례를 적용한다. 결측 센서는 보간에서 제외하고 센서 라벨에 ?와 --°C를 표시하며, 전체 센서가 결측이면 열화상은 중립 회색이다. 화면의 “예시 배치 / 센서값 보간” 안내로 설치 공간과 보간 온도의 성격을 명시한다. 저해상도 열화상은 센서값·위치·공간 범위·범례 입력이 바뀔 때만 캐시를 다시 만들며, 재생 중에는 센서 위치에서 퍼지는 마스크를 갱신한다. components/drawEnvironmentThermalGround.ts는 캐시한 이미지를 18×12 격자의 삼각형으로 나눠 같은 공장 카메라에 투영한다. 합성 버퍼와 격자 정점은 재사용하고 near plane 뒤의 정점이 포함된 삼각형·화면 밖 삼각형은 제외한다.
- 공간의 센서 설명창은 infoPanelFrame의 telemetry 외곽·탭·해칭과 drawEnvironmentLink의 강조 연결선을 실제로 재사용한다. 설명창 내부는 배경을 채우지 않아 설비와 열화상이 그대로 보인다. 센서 핀은 공장 투영 후 화면 오프셋을 적용하며 라벨은 회전하지 않는 화면 좌표에서 배치한다. environmentHeatmapLabels는 입력 센서 순서를 유지한 88×38 라벨을 기본 센서 위쪽 위치에 두고, 겹치면 가장 가까운 빈 후보를 선택해 최소 4 논리 좌표 간격을 확보한다. 설치 공간·제목·온도 범례의 영역을 침범하지 않도록 화면 안에서 라벨을 배치한다.
- 온습도 영향 경로: zoneEnvironment.ts(입력·관리 범위 판정·상하 고정 anchor·52초 단계) → environmentGauge.ts(고정 원형 배치·링 조립·연속 회전) / environmentLayout.ts(카드·그래프·원형 테두리 연결점) → components/drawZoneEnvironment.ts(카드·계기 조립) / components/drawEnvironmentGauge.ts(측정 아크·관리 범위·외곽 회전) / components/drawEnvironmentLink.ts(강조 연결선); temperatureHistory.ts(24시간 창·중복/누락 처리·공통 눈금) → components/drawZoneTemperatureHistory.ts(상하 카드 안쪽 이력 그래프). environmentHeatmap.ts는 센서 설치 좌표·공통 온도 범례·IDW 보간을, environmentHeatmapProjection.ts는 상공→하강→전진→선회 카메라·factoryState·센서 투영·라벨 충돌 회피를 담당한다. components/drawEnvironmentThermalField.ts(색 필드 캐시·노출 마스크) → components/drawEnvironmentThermalGround.ts(18×12 원근 격자·합성 버퍼) → components/drawEnvironmentSpace.ts(drawSmtFactory와 같은 원근으로 공간 조립) → components/drawEnvironmentHeatmap.ts(센서 가시성·라벨·범례) 순서로 연결한다. components/drawEnvironmentSensorCallout.ts는 infoPanelFrame.ts의 telemetry 프레임과 drawEnvironmentLink.ts를 공유한다. drawWaveFilm.ts가 단계별 제목·온도 통계·장면을 구성하고 filmProgram.ts가 52초 재생·30초 그래프 미리보기를 등록한다.
- 온습도 데이터 계약: zones에는 id/name/temperature/humidity/temperatureRange/humidityRange와 선택적인 temperatureHistory: { at: Unix 밀리초, value: number | null }[]를 전달한다. 선택적인 historyEnd는 공통 24시간 창의 끝이며 생략하면 전달 이력 중 가장 최근 시각을 사용한다. 이력이 없으면 대기 문구를 표시하고 null·비유한 값·2시간을 넘는 샘플 공백은 선으로 연결하지 않는다. 현재 측정값의 null·비유한 수·물리 범위를 벗어난 습도는 정상 판정에서 제외한다. 공통 플레이어 데이터 API는 아직 설계 단계다. wave ID와 온도계/물방울 메뉴 아이콘은 유지한다. cinemaEnvironmentGauge.test.ts는 고정 크기·원형 접점·연속 회전·정지/역방향 재현을, cinemaZoneEnvironment.test.ts·cinemaTemperatureHistory.test.ts·cinemaEnvironmentHeatmap.test.ts는 상하 5개 배치·중앙 연결점·그래프 경계·값 전달·누락·시간 창·역방향 탐색·단계 순서·보간·공통 색 범례를, cinemaEnvironmentHeatmapProjection.test.ts는 실제 factoryProject와의 일치·상공 센서 가시성·설비 위 비행 경로·시점 연속성·라벨 간격을 검증한다.
- 바이저 3D는 평면과 같은 8공정 순서의 5개 라인·40대를 원근 공간에 유지한다. 정면의 기준은 PCB 로더가 있는 라인 입구다. 로더를 가까이 두고 스크린 프린터→SPI→마운터→MAOI→리플로우→AOI→언로더가 화면 안쪽으로 이어지며, 5개 라인은 좌우로 나란히 놓인다. 카메라는 낮은 원근 시점으로 통로를 따라 이동하며 전체를 한 화면에 축소하지 않는다. 48초 동안 LINE 01 리플로우→LINE 03 MAOI→LINE 05 마운터를 방문한다. 각 16초 구간은 0~3초 입구, 3~4초 통로 진입, 4~6초 전진, 6~12초 진단, 12~14초 입구 후퇴, 14~14.7초 라인 중심 복귀, 14.7~16초 입구 횡단 이동이다. 설비 영역을 가로질러 다음 라인으로 이동하지 않는다. 카메라 앞 절단면을 통과하는 바닥·면·선은 클리핑하고 조작면의 반대편에는 글자 없는 후면을 그린다. 추적과 연결점은 같은 카메라를 사용한다.
- 차트는 선택 막대/파이 조각이 앞으로 다가왔다가 돌아간다. 막대의 원래 기준선·목표와 기준 윤곽은 남겨 확대가 수량 증가로 읽히지 않게 한다. 선택 부품에서 나온 연결선은 투영 후 실제 좌표를 사용하고 데이터 비율은 유지한다.
- 기본 테마는 배경 #040b10과 cyan/amber 계열이며 초점과 문장을 순차적으로 드러낸다. 정보 콘솔은 요청된 박스형 창을 사용하되, 외곽선이 그려진 뒤 제목·라벨·값이 순차적으로 찍히며 회전 부품과 연결선으로 이어진다.
- 모든 장면은 페이지 전체를 채우는 하나의 Canvas에서 배경·공간·정보를 함께 그린다. 외곽 여백, 16:9 화면 틀, 질감의 사각 테두리를 두지 않는다. 객체의 1280×720 논리 구성은 가로·세로에 같은 배율을 적용해 기어·숫자·차트가 찌그러지지 않게 하고, 배경과 조명은 논리 구성 밖의 실제 화면 끝까지 이어진다. 기존 HUD 위젯의 깊이 규칙은 이 실험의 선 좌표 변형에 적용하지 않는다.
- 같은 치형 간격을 가진 기어 3개가 접점을 공유한다. 인접 기어는 반대 방향으로 돌고 잇수 비율로 속도를 맞추며, 내부 문자는 고정해 가독성을 유지한다.
- 온도·생산량·양품률이 회전 흐름을 따라 순차적으로 드러난다. 위험 신호와 연결선도 동일한 장면 시간축을 사용한다.
- 스캔은 공정의 이상 설비로 시선을 모으고, 펼침은 숫자 자체를 차트로 변형해 종합 화면으로 연결하며, 추적은 워크오더의 공정 진행과 실적을 보여준다. 연출마다 관찰 목적과 시각적 변화를 구분한다.
- 지표 펼침은 32초 동안 큰 숫자 카운트업 → 동일한 빛 조각의 차트 변형 → 곡선 이동·축소 → 생산 종합 순서로 진행한다. 고정된 네 방향 전개는 사용하지 않는다. 0~2초 도입 후 생산 달성 87.4%, 양품률 96.4%, 사이클 8.2 s, 생산 잔여 183 EA를 6초씩 중앙에서 읽는다. 각 슬롯에서 0으로 등장한 숫자는 0.55~1.95초에 목표값까지 증가하며 끝으로 갈수록 감속한다. 소수점 한 자리와 정수 표기를 유지하고, 자릿수가 늘어도 소수점과 단위 위치를 고정한다. 목표값을 2.5초까지 유지한 뒤 2.5~4초 차트 변형·4~5.7초 정착으로 이어지며, 완성된 차트는 두 열의 서로 다른 크기와 높이로 누적한다. 26~30초에 목표 1,450−실적 1,267=잔여 183 EA를 차트 사이에 연결하고 30~32초 마무리한다. 이전 차트는 새 지표 판독 중 밝기를 낮추고 종합에서 복원한다.
- 펼침의 생산 달성은 실제/목표 비율의 분할 막대, 양품률은 96.4% 링과 기준 99.2% 표시, 사이클은 0~10초 눈금 위 8.2초 실측·7.1초 기준, 잔여는 실적과 잔여로 나뉜 생산 계획 막대다. 숫자 윤곽의 720개 점이 차트의 같은 점으로 연속 이동하며 색·형태·위치를 연결한다. 차트는 실제 값의 비율을 지키고 새로운 추세값을 만들지 않는다. 배경은 투명하며 기존 절단형 정보창의 윤곽과 탭만 차트가 완성될 때 조립한다.
- 펼침 영향 경로: unfoldMetrics.ts(수치·카운트업 표시값·32초 상태·차트 정착) → ribbonNumberGeometry.ts(기존 숫자 윤곽 공유) / metricMorphGeometry.ts(증가하는 숫자 점·수치 비율의 차트 점·연속 변형) → components/drawUnfoldMetric.ts(빛 조각·숫자 두께·투명 윤곽) / drawMetricChart.ts(4종 차트 눈금·기준선·주석) → drawUnfoldFilm.ts(순차 조립·종합 연결). 카운트업은 공통 시간에서 계산하고 지표마다 최근 표시값의 윤곽만 캐시하며, 목표값에 도달하면 차트 변형의 원본 점을 그대로 사용한다. filmProgram.ts와 FilmChapterIcon.tsx가 메뉴·32초 재생·28초 미리보기를 연결한다. cinemaUnfoldMetrics.test.ts와 cinemaMetricMorph.test.ts는 수치 정합·증가 순서와 소수 정밀도·숫자/차트/이동 순서·정착 누적·변형 끝점·정지/역방향 탐색을 검증한다. drawRibbonNumber.ts의 기존 숫자 표현은 같은 윤곽을 재사용한다.
- 모든 장면 하단에 16개 연출을 나타내는 육각형 아이콘 메뉴를 고정한다. 각 아이콘에는 짧은 이름을 붙이고 현재 장면은 밝은 윤곽과 앞으로 떠오른 두께로 표시한다. 마우스·키보드로 바로 장면을 선택하고, 전체 연속 재생 중에도 현재 아이콘이 바뀐다. 좁은 화면은 4열 4행과 하단 여유 390px을 사용하고, 높이가 낮은 화면은 가로 스크롤 한 줄로 전환한다. 메뉴 위에 장면 이름·경과 시간·재생·연출 설정을 표시한다. 전체 연속 / 현재 장면 반복, 탐색 슬라이더, 0.25~4배속, 질감, 처음부터 재생은 연출 설정 안에서 제공한다.
- 하단 재생 속도 선택(0.25 / 0.5 / 0.75 / 1 / 1.5 / 2 / 3 / 4배)은 장면 시간축 전체에 즉시 적용한다. 변경 시 현재 위치와 일시정지 상태를 유지하고, 처음부터 재생해도 선택 속도를 유지한다. 상단 루프 시간은 선택한 배속에 맞춰 표시한다.
- 연출 화면을 클릭하거나 터치하면 일시정지하고 다시 누르면 같은 위치에서 이어서 재생한다. SignalFilm.tsx의 Canvas 위 투명 버튼이 기존 togglePlay를 공유하며 모든 장면·질감·초점이 함께 멈춘다. 키보드 초점이 있는 화면 버튼의 Enter/Space도 같은 동작이다. FilmDock은 별도 형제 영역이므로 메뉴·설정·슬라이더 조작으로 화면 재생 상태가 중복 변경되지 않는다.
- 색상 테마는 연출 설정에서 시안 / 에메랄드 / 블루 / 앰버 / 로즈를 선택한다. 색상 견본·이름·선택 체크를 함께 표시한다. 기본 시안은 원래 Canvas 색을 보존하고, 나머지 테마는 배경·객체·문자·차트·질감·하단 메뉴를 함께 바꾼다. 경고는 기본적으로 앰버를 유지하며 앰버 테마에서는 붉은색으로 구분한다. 색 변경은 재생 위치·정지·배속·차트 형태를 바꾸지 않고 장면 이동·재시작에도 유지된다. 새로고침하면 기본 시안으로 돌아온다.
- 테마 영향 경로: filmThemes.ts(5개 테마와 기본·경고색) → FilmThemeControls.tsx(선택) → useFilmPlayback.ts(상태와 장면 공용 시계) → filmThemeCanvas.ts(Canvas 색·그라디언트 변환) 및 filmTexture.ts(테마별 재질 캐시). SignalFilm.tsx의 CSS 변수는 같은 테마를 하단 메뉴·설정·초점 테두리에 전달한다. 색상 어댑터는 플레이어별로 생성하고 Canvas 전역 프로토타입은 변경하지 않는다. 질감 Canvas는 테마별 최초 사용 시 한 번 생성해 재사용한다.
- 화면 질감은 없음 / 유리 / 필름 / 홀로그램을 선택하고 강도를 0~100%로 조절한다. 기본은 유리 55%. 모든 장면에 동일하게 적용하며 없음 또는 0%에서는 원래 렌더링을 그대로 표시한다. 장면 선택·배속·처음부터 재생 시 설정을 유지하고 일시정지 상태에서도 질감을 바꿔 같은 장면을 비교할 수 있다. 새로고침 시 기본값으로 돌아온다.
- 질감 영향 경로: FilmTextureControls.tsx(선택·강도) → useFilmPlayback.ts(설정 상태와 공통 재생 시간) → filmTexture.ts(Canvas 최종 합성). 전용 렌더러는 화면 전체의 유리 반사·필름 입자·홀로그램 결을 표현하며, 설비 개별 면의 물리 기반 재질은 적용하지 않는다. 생성한 패턴과 작은 합성용 Canvas를 재사용하고 질감 움직임도 공용 시계를 따른다.
- components/drawInfoPanel.ts는 위치·크기·제목·행 데이터·문자 속도·로컬 시간을 받아 박자형 텍스트와 커서를 그린다. drawGear.ts는 기존 기어 연동 장면에서도 공유한다. drawRotor.ts는 분할 링·타원 궤도·스캔 회전을 제공한다. 모든 부품은 자체 타이머 없이 전달받은 시간으로 그리므로 배속·일시정지·탐색과 함께 동작한다.
- 바이저 시점은 사용자 참고 영상 [All Iron Man HUD Scenes (up to Endgame)](https://www.youtube.com/watch?v=E7-o6a0OUHY)의 대표 구간(0:10, 3:18, 8:16, 13:14)을 관찰한 뒤 만든 독립 벡터 연출이다. 영상 파일과 영화 이미지는 포함하지 않는다. 주변 곡선 계기, 물체에 붙는 타깃 표시, 초점에 따른 정보 강조를 적용한다.
- 생산 라인은 x/y/z 좌표를 가진 공간이다. 바닥·천장·벽·구조물·설비를 동일한 원근 카메라로 투영하며, 카메라의 이동과 회전에 따라 가까운 물체가 크게 이동하고 먼 물체는 작게 움직인다. 확대는 카메라가 설비 쪽으로 전진하는 동작이며, 설비 윗면·옆면과 앞뒤 겹침이 깊이를 드러낸다. 선·면과 거리별 명암으로 공간을 표현한다.
- 대상 괄호는 설비 부피의 투영 경계에서 계산하고 연결선은 같은 3D 열원 좌표를 사용한다. 시야 가장자리 계기는 바이저 좌표에 남는다. 중앙은 탐색 → 고정 → 접근 → 이상 분석 → 정상 복귀 순서로 읽으며, 강조색은 상태 변화 때만 cyan에서 amber로 이동한다.
- drawVisorFilm.ts는 시간과 초점·데이터를 조립한다. inspectionSpace.ts는 카메라·원근 투영·시야 앞면 클리핑·설비 위치를 공유하고, drawInspectionRoom.ts는 공간, drawInspectionField.ts는 깊이 순서의 설비 면을 그린다. drawVisorOverlay.ts는 주변 계기, drawTargetReticle.ts는 추적 괄호를 제공한다. 원근 축소·이동 시차·클리핑·전체 카메라 경로는 cinemaInspectionSpace.test.ts에서 검증한다.
- 바이저 3D 정보는 drawVisorReadout.ts의 VISOR_READOUT_DATA에서 읽는다. SMT 03은 박스형 온도·냉각 정보, SMT 04는 투명한 두께와 회전 아크가 있는 품질 링(1,262/1,280 = 98.6%), SMT 01은 3개 시간대의 가로 입체 막대(860/720/940 EA, 시간당 목표 900 EA, 공통 축 0~1,000 EA)를 펼친다. 각 부품은 360×320 안에서 3초 내 수치가 완성되며, 연결점은 부품의 실제 테두리 좌표에서 가져와 확대 투영 후 연결한다. 색상 테마·질감·배속·탐색 시점은 기존 플레이어와 공유한다.
- 바이저 평면의 냉각부 온도·팬 출력·양품률과 상태는 visorTelemetry.ts의 같은 시뮬레이션에서 읽으며, 추세선도 그 함수의 과거 60초를 그린다. 냉각 중에는 COOLING을 유지하고 기준 온도 복귀 후에만 STABLE/VERIFIED를 표시한다. drawPlanarVisorFilm.ts와 drawInspectionPanorama.ts가 정면 생산 라인의 이동과 리플로우 06 냉각팬의 열원·추적 경계를 함께 계산한다.
- 바이저 3D 영향 경로: smtLine.ts(평면·3D 공통 8공정) → smtFactory.ts(5라인·40대 위치·입구 이동 경로·공정축을 깊이로 변환하는 factoryWorld·추적 경계) → components/drawSmtFactory.ts / drawSmtEntrance.ts(입체·PCB 투입면) / drawSmtEquipment.ts / drawProjectedFilmSurface.ts(공통 조작면 도형·원근 표면) → drawVisorTourFilm.ts(장면 조립) / drawVisorReadout.ts(선택 라인·설비명과 박스·원형·가로 막대). filmProgram.ts는 48초와 입구 미리보기 1.5초를 사용한다. 검증: cinemaSmtFactory.test.ts에서 40대 고유성·공정 깊이 순서·입구 원근 크기·라인 좌우 순서·통로 여유 거리·카메라 이동 연속성·진단 추적 정합을 검사한다.
- 바이저 3D에서 직접 탐색 버튼·마우스 조작은 현재 시점을 유지하며 자동 시간을 정지한다. 왼쪽 드래그는 시점 회전, 휠은 거리 조절, Shift 또는 오른쪽 드래그는 바닥 이동, 짧은 클릭은 가장 가까운 설비 본체 선택이다. 선택은 시안 입체 테두리와 라인·공정 순서·설비명으로 표시한다. 5개 라인·40대 목록과 선택 설비로 이동 버튼은 화면 밖 설비 접근 및 선택 설비 중심 회전을 제공한다. 입구 시점은 최초 로더 앞 시점을 복원하고, 자동 투어/재생은 수동 시점을 해제하고 정지했던 시간에서 재개한다. 장면 선택·타임라인 탐색·처음부터·메인 메뉴에서도 수동 선택을 해제한다. 방향키 회전, Shift+방향키 이동, +/- 거리 조절, Home 입구, Escape 선택 해제, F 선택 설비 이동을 지원한다. 다른 연출은 기존 화면 클릭 재생·정지를 유지한다.
- 직접 탐색 영향 경로: smtFactoryInteraction.ts(orbit 카메라·범위 제한·ray와 본체 교차 선택) → useSmtFactoryInteraction.ts(렌더 ref와 선택 상태) → SmtFactoryExplorer.tsx(포인터 캡처·6px 드래그 구분·키보드·휠) / SmtFactoryControls.tsx / smtInteraction.module.css(선택·복귀 UI) → SignalFilm.tsx / useFilmPlayback.ts(조립·시간 정지·장면 전환) → drawSignalFilm.ts / drawVisorFilm.ts / drawSmtExploreFilm.ts(공통 설비 렌더·선택 테두리). filmViewport.ts의 동일 변환을 그리기와 클릭 역변환에 사용해 DPR·도크·초광폭 화면 가장자리에서도 선택 좌표를 맞춘다. cinemaSmtInteraction.test.ts는 40대 선택·가림·화면 가장자리·초점·회전/팬/줌 한계·자동 카메라 인계를, cinemaFilmViewport.test.ts는 7가지 화면과 DPR 1/2의 포인터 역변환을 검증한다.
- 전체 화면 영향 경로: SignalFilm.tsx / film.module.css(모든 장면에 동일한 전체 화면 Canvas) → useFilmPlayback.ts(도크 높이를 실제 Canvas 픽셀로 환산) → drawSignalFilm.ts(inset 전달) → filmViewport.ts(균일 배율과 실제 화면의 역투영 경계) → 각 draw*Film.ts / drawChartStage.ts(전체 배경·광원). visorViewport.ts는 같은 함수를 재내보내 바이저도 동일 규칙을 사용한다. 차트는 6번째 presentation과 7번째 inset을 구분해 전달한다. 정보는 도크 위에 배치하고 배경·질감·장면 전환 암전은 도크 뒤까지 전체 물리 Canvas를 덮는다. 영화·유리·홀로그램 모두 외곽 사각 테두리를 그리지 않는다.
- FilmDock.tsx는 일반 장면과 바이저 양쪽에서 같은 위치에 유지되는 하단 제어부다. FilmChapterMenu.tsx는 장면 목록과 선택 상태를 육각형 버튼으로 연결하고, FilmChapterIcon.tsx는 각 연출의 특징을 담은 SVG 선 아이콘을 제공한다. FilmControls.tsx는 펼친 설정 내용만 담당한다. 장면 선택 시 설정은 닫히고 속도·질감·반복 방식은 유지된다. Escape로 설정을 닫으면 설정 버튼으로 키보드 초점을 돌린다.
- 도크의 드롭다운·슬라이더는 FilmFields.tsx의 `SelectField`·`RangeField`·`PercentField`만 사용한다(라벨+컨트롤+값 표시, `—`는 값이 무의미한 슬라이더 표기). 개별 컨트롤 파일에 `<label><select>`·`<input type=range>`를 직접 쓰지 않는다. 영향 경로: FilmFields.tsx(마크업·aria) → FilmControls.tsx / FilmTextureControls.tsx / FilmChartControls.tsx / FilmMachineControls.tsx / FilmCameraControls.tsx → film.module.css(.speedControl / .textureIntensity / .textureValue). 검증: cinemaMachineControls.test.ts, cinemaPersistentDock.test.ts.
- 자동 드리프트 스크롤(좌우 지표 스트립, 좌/우 홀로그래픽 스트림)은 useDriftScroll.ts 하나를 공유한다. 호버·초점·직접 스크롤·정지 버튼·숨김 탭·동작 줄이기에서 멈추고 끝에서 방향을 바꾼다. 영향 경로: useDriftScroll.ts → JarvisMetricCards.tsx(axis x) / JarvisStream.tsx(axis y + onFrame 포즈).
- 브라우저의 `/api/cinema/*` 호출은 cinemaApi.ts(`cinemaApiUrl`·`cinemaApi`·`CINEMA_BASE_PATH`)를 거쳐 basePath를 한 곳에서 적용한다. 영향 경로: cinemaApi.ts → useJarvisVoice.ts / useJarvisLocalVoice.ts / jarvisRealtimeSession.ts / admin/HatcheryAdmin.tsx / feedPolling.ts / staticSceneData.ts.
- 렌더 루프 성능 규칙: (1) 장면 수학 헬퍼(clamp·mix·smoothstep·pad2)는 filmMath.ts, 텍스트 절단은 filmDrawing.ts `fitText`(이진 탐색+캐시)를 쓰고 파일마다 복사하지 않는다. (2) 프레임마다 같은 인수로 만드는 그라디언트는 컨텍스트·테마 키로 캐시한다(drawJarvisBackdrop.ts, drawPieChart.ts 벽면은 단위 그라디언트+transform). (3) 오프스크린 버퍼는 실제 사용 영역만 clear한다(drawProjectedFilmSurface.ts). (4) 기본 cyan 테마는 filmThemeCanvas.ts가 Proxy 없이 원본 컨텍스트를 돌려준다. (5) useFilmPlayback.ts는 미리보기 중이거나 0.1초 단위 위치가 같으면 React에 position을 publish하지 않는다. 검증: tests/unit/scenes/cinemaFilmFingerprint.test.ts가 16개 장면 × 3시점의 draw-call 지문을 고정한다. 순수 리팩터는 지문이 그대로여야 하고, 의도한 시각/성능 변경만 스냅샷을 갱신한다.
- 바이저는 하단의 '바이저 3D'(원근 공간 아이콘)와 '바이저 평면'(좌우 이동 아이콘)이라는 두 개의 독립 메뉴로 바로 선택한다. 각 항목은 visor/visorPan 장면과 고정된 space/pan 렌더 방식에 연결된다. 표시명은 filmProgram.ts에서 가져와 메뉴·현재 장면·접근성 이름을 통일한다.
- 막대 비교와 파이 구성은 chartData.ts의 동일한 5개 라인 수량(860/720/940/610/790 EA, 합계 3,920 EA)을 사용한다. 막대는 0부터 시작하는 같은 눈금과 라인별 목표 800 EA를 사용하고, 파이 각도는 수량/합계로 계산한다. 중심이 채워진 파이이며 숫자 표기는 소수점 한 자리로 반올림한다.
- drawBarChart.ts는 채널별 T1~T5 헤더, 48단 발광 스트립, 평행 빛 레일, 국소 눈금과 목표 표시로 생산량을 그린다. 3D에서는 각 층의 측면과 윗면이 투명하게 이어지고, 2D에서도 분할 발광과 스캔은 유지한다. 스캔은 빛만 움직이며 수량을 임의로 바꾸지 않는다. 선택 막대는 원래 윤곽과 공통 0 기준을 남기고 앞으로 확대되며, 함께 움직이는 눈금은 값과 목표 범위까지 표시해 고정 채널 헤더를 침범하지 않는다. 오른쪽에는 합계·목표 대비 달성률·부족량과 분할 게이지, 아래에는 라인 목표·전체 달성률·목표 도달 라인 수를 표시한다.
- 막대 영향 경로: chartData.ts(동일한 생산량·목표) → drawBarFilm.ts(장면·분석·요약) → components/drawBarChart.ts(채널 배치·등장·포커싱) → barTelemetryGeometry.ts(공통 축·분할 높이·실제 투영 연결점), components/drawTelemetryBar.ts(발광 층·스캔·입체 면·국소 눈금). 마지막 분할은 실제값에서 잘리고 상단 캡은 눈금 사이에서도 정확한 높이를 가리킨다. 모든 움직임은 기존 재생 시계 하나를 사용한다.
- drawPieChart.ts는 두께 있는 반투명 유리 조각이 부상하며 조립되고, 회전 뒤 선택 조각이 바깥과 위로 분리되는 입체 파이 부품이다. 윗면·절단면·곡면의 원근과 깊이 순서, 가장자리 반사광과 바닥 반사로 부피를 드러낸다. 막대와 파이는 데이터·위치·크기·선택·시간을 받아 다른 장면에도 사용할 수 있고 파이는 두께와 기울기도 받는다. 두 부품은 실제 그려진 요소의 연결점 좌표를 반환한다.
- 차트 장면은 drawChartStage.ts의 낮은 밝기 공간을 공유하고, 정보창 테두리 없이 차트 자체가 주인공이 된다. drawBarFilm.ts / drawPieFilm.ts가 도입 → 전개 → 선택 → 설명 → 정리의 시간을 조립한다. 배속·정지·탐색은 기존 공용 시계를 따른다.
- 막대·파이 장면의 연출 설정에는 각각 2D 평면 / 3D 입체와 입체 두께 40~160%를 제공한다. 기본은 3D, 두께 100%. 2D에서는 측면과 두께를 없애고 파이를 정면 원형으로 그리며, 선택 대상의 접근·확대·복귀는 유지한다. 두께는 3D에서만 조절하며 2D로 전환했다 돌아오면 선택한 두께를 복원한다. 데이터·현재 재생 위치·정지 상태를 유지하여 같은 시점을 비교할 수 있다.
- 차트 설정 영향 경로: chartPresentation.ts(공유 형태·두께 범위) → FilmChartControls.tsx(현재 차트의 설정) → useFilmPlayback.ts(막대·파이 독립 상태) → drawSignalFilm.ts → drawBarFilm.ts / drawPieFilm.ts → components/drawBarChart.ts / drawPieChart.ts. 장면 전환·배속·처음부터 재생 시 유지하고 새로고침 시 기본값으로 돌아온다. 화면 전체 질감 설정은 차트 형태와 별개로 적용한다.
- 파이 장면은 왼쪽의 큰 입체 조각과 오른쪽의 선택 비중에 시선을 모은다. 선택 조각은 앞쪽 오른편으로 회전한 뒤 들려 올라오며, 그 조각의 실제 투영 좌표에서 설명으로 연결선을 잇는다. 전체 합계와 선택 비중·수량을 순차적으로 드러내고, 나머지 라인은 하단의 짧은 요약으로 표시한다. 유리 조각의 두께 표현은 장면 부품 자체의 재질이며, 공통 화면 질감 설정과 별도로 유지한다.
- 육각형 메뉴는 넓은 화면에서 장면 개수에 맞춘 한 줄, 680px 이하에서 4열 4행으로 표시한다. 열의 최소 너비를 유지하고 공간이 부족하면 메뉴 안에서 가로로 스크롤한다. 높이 500px 이하에서는 작은 한 줄 메뉴를 사용한다. 모든 장면의 배경은 페이지 전체를 채우며 정보는 하단 메뉴 위의 영역에 균일 투영한다. 설정은 화면 높이에 맞춰 자체 스크롤하며 모션 줄이기 환경에서는 메뉴의 전진 전환을 생략한다. cinemaFilmViewport.test.ts에서 가로·세로·초광폭 화면과 DPR별 배경 덮기·비율·도크 여백을 검증한다.
- 코너 전개는 생산 달성 → 양품률 → 설비 가동 → 사이클 타임을 중앙에 한 개씩 확대 표시한다. 각 정보는 1.6초에 걸쳐 다가와 판독 시간을 주고, 곡선을 따라 깊이 방향으로 물러나며 오른쪽 위 → 오른쪽 아래 → 왼쪽 위 → 왼쪽 아래 순서로 정착한다. 코너에서는 좌우 약 40도·상하 약 7도로 회전해 화면 중앙에 가까운 가장자리가 더 멀고 작게 보인다. 왼쪽 면은 오른쪽 가장자리가, 오른쪽 면은 왼쪽 가장자리가 안으로 들어가는 대칭 구조다. 먼저 이동한 정보는 유지한다. 네 정보가 모두 정착한 뒤 29.2초부터 중앙에 생산 종합을 확대하고, 37.5~40초에 전체 정보가 뒤로 물러나며 사라진다.
- 코너 전개 영향 경로: cornerSequence.ts(4개 데이터·목표/실적·위치·공용 시간) → drawCornerFilm.ts(투영·이동 잔상·연결선·최종 배치) → drawCornerReadout.ts(4가지 정보 부품), drawHolographicCore.ts(원형 중앙 종합), drawCornerField.ts(화면 전체 광학 배경). 배경은 방사선 없이 희미한 곡선 8개만 남기고 조명과 입자로 공간감을 표현한다. 연결점은 정보 면의 실제 윤곽에서 가져온다. 중앙의 183 EA는 목표 1,450−실적 1,267에서 계산하며 생산 달성 87.4%와 같은 원본을 사용한다. filmProgram.ts와 FilmChapterIcon.tsx가 독립 메뉴·40초 반복·전체 연속 재생에 연결한다.
- 마지막 중앙 정보는 보라빛 플라스마 링과 청록색 눈금·분절 링이 겹치는 원형 코어다. cornerCoreGeometry.ts의 같은 원근 투영으로 앞뒤 테두리·투명 측면·눈금·네 연결점을 계산하고, 전달된 장면 시간으로 링의 역방향 회전과 세 개의 밝은 파동을 움직인다. 값·단위·목표 달성률은 읽기 영역에 고정한다. 29.2~31.8초 접근·확대와 37.5~40초 후퇴·축소를 유지하며, 코어의 440×440 범위를 기존 네 귀퉁이와 겹침 검사한다. drawHolographicCore.ts는 제목·값·단위·진행률·설명과 시간을 받아 다른 장면에서도 재사용할 수 있다.
- 코너의 실제 원근 좌표는 cornerProjection.ts가 위치·깊이·회전과 공통 소실점에서 계산한다. drawProjectedFilmSurface.ts는 재사용 캔버스에 그린 면·글자·차트 전체를 같은 격자 투영으로 합성하고, 연결점도 동일한 투영 함수를 쓴다. filmThemeCanvas.ts에서 현재 색상 테마를 물려받으며, 시간 탐색과 일시정지 때도 같은 기하를 재현한다.
- 정착한 네 정보는 2.4초에 걸쳐 부유 움직임을 시작한다. 각기 다른 위상으로 상하 약 7·좌우 약 3.5 논리 좌표, 깊이 약 20과 작은 회전을 더해 천천히 흔들린다. 움직임은 장면 시간에서 계산하며 연결선도 같은 투영에 붙어 움직인다. 중앙 핵심 정보는 안정적으로 유지한다.
- HATCHERY 메인 메뉴는 /cinema의 기본 인트로다. 상단은 생산 진행·공정 병목·SPC 이탈·사용 전력을 요약하며 jarvisMainData.ts가 기존 공정·에너지·품질·검사 시연 데이터를 함께 참조한다. 중앙 음성 파동·대화·명령 입력은 독립 영역이다. 좌측은 운영자 영상·연결·생산·공정·병목, 우측은 품질·에너지·검사·온도 이탈 정보를 세로로 순환한다. JarvisStream.tsx는 복제 없이 각 목록을 15/19px/s로 자동 스크롤하며 끝에서 1.8초 머문 뒤 역방향으로 돌아온다. 마우스·키보드 포커스·직접 조작 중에는 멈추고 정지/재개 버튼을 제공한다. 화면 비활성·동작 줄이기 환경에서는 자동 이동을 멈추며 언마운트 시 RAF를 해제한다. 영향 경로는 jarvisMainData.ts → JarvisOperations.tsx / JarvisTemperatureAlerts.tsx → JarvisStream.tsx → JarvisMain.tsx, 스타일은 jarvisStream.module.css / jarvis.module.css다. 온도 알림은 공통 environmentReadingStatus로 온도 이탈만 판정하며 전체 ZONE 목록은 상세 연출로 연다. 현장 요약은 같은 생산·공정·품질·전력 값을 설명하고 온습도 질문만 환경 요약으로 답한다. 모든 수치는 시연 데이터다. 메인 메뉴 진입 시 장치를 자동 시작하지 않으며 다른 연출로 이동하면 장치를 해제한다.
- 메인 카메라 경로: filmCameraSession.ts(영상 권한·스트림 소유·취소/종료) → useFilmCamera.ts → JarvisMainHeader.tsx/JarvisCamera.tsx. 대화 시작 또는 내 영상 연결이라는 명시적 동작에서만 요청하며 초기 화면은 장치 꺼짐이다. 영상은 우측 상단 고정 영역에 비율을 유지해 표시하며 녹화·업로드하지 않는다. 카메라 요청은 audio:false이고 마이크는 별도 세션이 소유한다.
- 얼굴 추적 경로: useFilmCamera.ts → filmFaceTracking.ts(최대 10fps, 축소 프레임·Worker 수명) → public/cinema/face-tracker.worker.js(MediaPipe FaceDetector) → faceTracking.ts(얼굴 선택·평활화·잠시 놓친 얼굴 유지). 모델과 WASM은 public/cinema/vision에 자체 호스팅하며 영상은 브라우저 밖으로 전송하지 않는다. 얼굴 위치 검출만 수행하며 신원 인식이나 배경 분리 모델은 사용하지 않는다. 로딩·탐색·추적·실패 상태를 구분해 표시한다.
- cameraPortrait.ts의 검출 얼굴 크롭을 JarvisCamera.tsx가 우측 상단 영상 영역에 적용한다. 얼굴 추적은 신원/생체 측정이 아니며 거울·확대·블러 설정을 유지한다. 이전 drawCameraFilm.ts/drawCameraPortrait.ts 원형 얼굴 HUD 렌더러는 재사용 소스로 남아 있지만 현재 기본 진입 화면에서는 사용하지 않는다.
- HATCHERY 연결 경로: useJarvisVoice.ts → jarvisRealtimeSession.ts → /api/cinema/realtime → 서버 전용 src/server/cinema/openai.ts → OpenAI Realtime. 영구 API 키는 .env.local에서 서버만 읽고 브라우저에는 SDP 응답만 전달한다. 음성 시작은 사용자 클릭이며 10분 상한·중지·페이지 숨김·장면 이탈에서 마이크/오디오/RTC/타이머를 정리한다. open_scene은 허용된 FilmId만 처리하고 최종 안내 응답의 오디오 재생 종료 후 전환한다. 입력창은 비활성 세션에서 /api/cinema/assistant의 로컬 명령 우선 → Responses API 자유 질문 경로를 사용하고 글로 답한다. 히스토리는 8개 이내, 응답 저장은 store:false이며 실제 MES 대신 시연 스냅샷을 제공한다. 카메라는 전송하지 않는다. 로그인 없는 데모의 유료 엔드포인트는 localhost 요청만 허용한다. 검증 기록: docs/reports/2026-09-07-jarvis-openai-integration.md.

- 12~15번은 이후 이름으로 지정해 재사용할 수 있는 독립 장면이다. 공통 holoSpace.ts의 원근 좌표를 모델·선택 부위·빛의 경로·연결점에 함께 적용한다. 각 장면은 입력 데이터, 순수 시간 상태, 그리기 부품, 장면 조립 파일로 분리한다. 렌더러의 마지막 인자에 데이터 스냅샷을 전달할 수 있고 자체 타이머·네트워크 연결을 만들지 않는다. 현재 메뉴는 기본 시뮬레이션 데이터를 사용한다.

| 메뉴 / ID | 데이터·상태 | 재사용 그리기 부품 | 장면 조립 / 동작 |
| --- | --- | --- | --- |
| 13 살아 있는 공정망 / network | processNetwork.ts · ProcessNetworkData / DEFAULT_PROCESS_DATA | components/drawProcessNetwork.ts | drawProcessNetworkFilm.ts · 데이터로 병목 선택, 방향을 가진 흐름·대기 입자, 접근·원인 분석·회복값 보간·전체 복귀 |
| 14 에너지 파동 / energy | energyCore.ts · EnergyCoreData / DEFAULT_ENERGY_DATA, energyDashboard.ts(2막 타임라인·팔레트·패널 배치·이력/매트릭스/점검 파생값·7세그먼트 인코딩) | components/drawHudPanel.ts(모서리 컷 유리 패널·7세그먼트 숫자), components/drawEnergyRing.ts(중앙 링: 톱니 링 ↔ 쐐기·점 궤도 모프), components/drawEnergyHudBoard.ts(1막: 막대+면적, 속도계, 링 게이지, 육각 판독, 홀로그램 받침대), components/drawEnergyInfoBoard.ts(2막: 요약, 파형, 비율 링, 도트 매트릭스, 점검표+그라데이션 바, 산형 추세) | drawEnergyCoreFilm.ts · 0~15초 사이언 HUD 대시보드(네 모서리 유리 패널이 밖에서 미끄러져 들어오고 링이 조립), 15~17.4초 링이 제자리에서 보라·마젠타·오렌지 인포그래픽으로 모프(톱니 → 점 궤도, 분절 호 → 쐐기, 배경 남색 → 보라)하며 패널이 교체, 17.4~30초 2막, 30~32초 페이드. 2초부터 4초마다 전력 → 생산 → 효율 채널을 순환하며 각 패널의 값·바늘·매트릭스가 그 채널을 따른다. 상단 중앙 7세그먼트 판독 띠는 두 막 공통. 자세한 규칙: 이 표 아래 에너지 2막 항목. |
| 15 제품 내부 검사 / product | productInspection.ts · ProductInspectionData / DEFAULT_PRODUCT_DATA | components/drawProductCutaway.ts | drawProductInspectionFilm.ts · 하우징·엔드캡 분리, 축·베어링·권선 검사, 실제값과 공차 비교, PASS/FAIL과 종합 판정 |
| 16 SPC 분석 / spc | spcTypes.ts · SpcData / spcData.ts · DEFAULT_SPC_DATA / spcStatistics.ts / spcScene.ts | components/drawSpcControlCharts.ts · drawSpcHistogram.ts · drawHolographicCore.ts | drawSpcFilm.ts · X̄–R 이상 부분군 확인 → 관리도 후퇴·기울임 → 실측 히스토그램 접근 → 양측 차트 후퇴·중앙 Cpk 코어 확대 → 전체 복귀 |

- SPC는 같은 크기의 부분군 2~10개 측정값, 최소 2부분군을 받는다. 기본 시뮬레이션은 외경 25부분군×5개=125개, LSL 9.950mm·USL 10.050mm·설정 Cpk 목표 1.33이다. 군 평균/범위, 3σ 관리한계, 군내 σ=R̄/d₂, Cp=(USL−LSL)/(6σ), Cpk=min((USL−평균)/(3σ),(평균−LSL)/(3σ))를 원시 측정값에서 계산한다. 공식은 [NIST X̄–R 관리도](https://www.itl.nist.gov/div898/handbook/pmc/section3/pmc311.htm), [NIST 공정능력](https://www.itl.nist.gov/div898/handbook/pmc/section1/pmc16.htm), d₂/d₃ 상수는 [Minitab 계수표](https://support.minitab.com/en-us/minitab/help-and-how-to/quality-and-process-improvement/control-charts/how-to/variables-charts-for-subgroups/r-chart/methods-and-formulas/estimating-sigma/)를 따른다.
- SPC의 관리한계는 전달된 모든 부분군에서 추정하고 이탈 부분군을 임의로 제외하지 않는다. 관리도에는 UCL/CL/LCL, 히스토그램에는 별도 입력인 LSL/USL과 실제 평균을 표시한다. 규격과 실측 범위를 포함한 12개 등간격 구간에 모두 집계하며 최댓값도 마지막 구간에 포함한다. 기본값은 SG-18 평균만 관리한계를 벗어나며 규격 밖 측정값 2/125개, Cp 1.517·Cpk 1.189이다. 정규성은 검증하지 않았고 3σ 한계 이탈 검사만 제공하므로 공정능력 합격을 단정하지 않는다. 공정 불안정은 참고값으로 표시하고 0군내분산은 Cp/Cpk를 산출 불가로, 음수 Cpk는 음수 그대로 보여준다. 불완전 입력은 전체 오류로 반환한다.
- SPC는 측정선이 쌓임 → 이탈 부분군 확대 → 실측 분포의 입체 막대 상승 → 공정능력 종합으로 시선을 옮긴다. 관리도는 계산된 UCL/LCL 사이에 투명한 범위 면을 두고, X̄는 중심선 편차·R은 0에서의 범위를 가는 기둥과 빛의 선으로 표현한다. 선의 앞쪽 좌표는 실측값이며 뒤쪽 5px 두께는 재질 표현이다. 순차로 그리는 선 끝에는 주사광을, 이탈점에는 앰버 선·십자 표적·한계 초과 길이를 표시한다. 선택 부분군의 원시값·평균·범위·실제 UCL 초과량은 기존 투명 절단형 정보창에서 읽고 연결선은 투영된 실측점에 붙인다. 수집 정보와 상세 정보는 순차로 사라지고 나타나 같은 위치에 글자가 겹치지 않는다.
- SPC 히스토그램은 실측 빈도에 맞는 앞면과 밝은 상단·측면을 사용하고 빈도 숫자가 막대와 함께 증가한다. LSL/USL은 앰버 게이트, 규격 내는 투명 청록 면, 규격 밖은 옅은 앰버 면으로 구별한다. 하단 원시 측정점은 실제 측정값의 가로 위치를 유지하며 겹침만 세로 줄로 분산한다. 규격 밖 원시점만 앰버로 칠하고 일부만 규격을 벗어난 구간의 막대 전체를 불량으로 표시하지 않는다. 세 단계 진행선과 공정능력 코어의 양옆 연결점은 공통 장면 시간을 따르며 실제 데이터·통계·40초 카메라 순서는 유지한다.
- SPC 영향 경로: spcTypes.ts(측정 규격) → spcData.ts(데모 입력) → spcStatistics.ts(통계·이탈·분포) → spcScene.ts(공통 시간·3D 면 투영) → drawSpcFilm.ts(연출 조립) → components/drawSpcControlCharts.ts(범위 면·한계·축) / drawSpcControlTrace.ts(측정선·순차 주사·이탈 표적) / drawSpcInspection.ts(선택 부분군의 투명 상세창) / drawSpcHistogram.ts(입체 빈도·원시 측정점) / drawSpcSequence.ts(단계 진행·코어 연결) / drawHolographicCore.ts(공정능력). 직접 렌더러에 SpcData를 전달할 수 있으며 플레이어 공통 데이터 API는 아직 설계 단계다. 코어의 선택적 progressLabel은 지수에 맞는 목표 문구를 제공하며 기존 달성률 문구의 기본 동작은 유지한다. cinemaSpcStatistics.test.ts는 수작업 계산·단위 변환·0변동·음수·경계 집계·오류 입력을, cinemaSpcScene.test.ts는 전 구간 표면 경계·축소·기울기·역방향 탐색을 검증한다.
- 재사용 영향 경로: 데이터 필드·규칙 변경은 각 데이터 모듈과 그 전용 테스트를 먼저 수정하고, 그리기 부품과 장면 내 라벨/단위/연결점에 반영한다. 새 장면 선택 항목은 filmProgram.ts → drawSignalFilm.ts → FilmChapterIcon.tsx / FilmChapterMenu.tsx에 함께 등록한다. 갤러리 테스트는 16개 ID·모든 장면 경계·538초 루프를 검사한다. 각 장면의 조회·회전·접근·복귀는 공용 시간만 사용하므로 0.25~4배속·정지·탐색이 같은 위치를 재현한다.
- 공정 회복은 보간 비율만으로 완료를 표시하지 않고 라인 처리 능력이 요구량을 충족할 때 완료로 표시한다. 제품 공차는 경계를 포함해 판정하며 누락·유효하지 않은 측정값을 PASS로 바꾸지 않는다. 그래픽 모델은 연출용 도형이며 CAD나 실제 설비 모델을 불러온 것은 아니다.
- 구현: src/cinema/SignalFilm.tsx (화면 조립), useFilmPlayback.ts (재생과 탐색), FilmControls.tsx (장면 선택과 제어), filmProgram.ts (장면 목록과 시간 계산), drawSignalFilm.ts (장면 렌더러 연결), draw*Film.ts (개별 연출), components/ (재사용 연출 부품), filmDrawing.ts (색·글꼴·보간), drawGearTrain.ts (기어와 정보), gearGeometry.ts (접점·회전 관계), film.module.css (외곽).




- 변화 추적은 24초 동안 워크오더 생성 → PCB 로더·스크린프린터·SPI·마운터·MAOI·리플로우·AOI·언로더 → 불량 분리와 양품 생산량 누적 → 작업 실적 확정을 보여준다. 중앙에서 생성된 지시 정보창이 좌측 상단으로 이동하고 같은 PCB 시리얼이 실제 설비 모양 아래의 흐름선을 따라 이동한다. MAOI는 마운터 뒤의 광학 검사다. 시안은 생산 흐름·양품, 앰버는 검사 불량이며 정보창 내부는 투명하고 기존 절단형 윤곽·탭을 유지한다.
- 변화 추적의 현재 장면 반복은 처음 진입할 때만 0~2.4초의 설비 등장과 지시창 이동을 보여준다. 이후에는 설비와 정보창이 완성된 2.4초부터 결과 표시가 끝나는 23초 직전까지 순환한다. 설비·집계 정보의 등장 투명도는 첫 PCB 투입 전에 완성하고, 반복 끝에서는 장면 암전에 진입하지 않는다. 매 순환에서 같은 시연 워크오더의 PCB 투입·검사·실적을 재생하며 누적값을 실제 생산 실적으로 이월하지 않는다. 전체 연속은 기존 24초 후 정보 콘솔로 이동하고, 장면 재선택과 처음부터 재생은 0초 도입을 다시 표시한다. workOrderTraceTiming.ts의 TRACE_TIMING·TRACE_LOOP를 filmProgram.ts와 연출이 공유하며, cinemaTraceLoop.test.ts와 cinemaFrameState.test.ts에서 다중 반복·시간 초과분·설비 및 정보창 유지·암전 방지·전체 연속을 검증한다.
- 변화 추적의 시연 워크오더 WO-260908-001은 지시 120 EA다. 0.6초 생성, 2.4초부터 0.06초 간격 투입, 공정당 1.25초 이동, 19.54초 마지막 언로드로 양품 116 EA·불량 4 EA가 확정된다. SPI 납량 부족 1개, MAOI 부품 위치 편차 2개, AOI 납땜 브리지 1개를 검사 시점에 분리하며 후속 공정으로 보내지 않는다. 대기+재공+양품+불량=지시 수량, 공정 진입=통과+불량+재공을 유지한다. 생산량은 언로더를 통과한 양품만 세며, 아래 누적 그래프도 동일 시뮬레이션의 양품·불량 값을 사용한다. 실제 MES 데이터는 연결하지 않는다.
- 변화 추적 영향 경로: smtLine.ts(공통 8공정) → workOrderTrace.ts(워크오더·시리얼·검사 이력·순수 시간 상태) → components/drawWorkOrderProcess.ts(설비·공정 연결·통과 수량) / drawWorkOrderReadouts.ts(투명 지시창·집계·검사 알림·누적 그래프) → drawTraceFilm.ts(생성·PCB 이동·불량 분리·완료 연출). filmProgram.ts와 FilmChapterIcon.tsx가 기존 05 변화 추적 메뉴에 연결한다. cinemaWorkOrderTrace.test.ts는 수량 보존·공정 순서·검사 시점·불량 이후 공정 제외·정지와 역방향 탐색 재현을 검증한다.
